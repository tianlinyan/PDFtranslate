# 更新日志（CHANGELOG）

本文件按版本倒序记录 PDFtranslate 的每一次功能变更与缺陷修复，供追溯「某行为为什么是这样」。
条目由 `README.md` 拆出，正文逐字保留（含内部实现细节、回归测试名与真机实测数据）。

* **当前版本**：见 `translate_app/__init__.py` 的 `__version__`（本文件最新条目即当前版本）。
* **设计文档**：`docs/`（阶段性设计与代码审查报告）。
* **README**：面向使用者的安装、配置与操作说明；架构与实现约束见 `CLAUDE.md`。
* **体例**：每条按「版本 → 症状/动机 → 根因 → 修法 → 验证（回归测试 / 真机复测）」组织。
* **历史断层**：v0.5.0 之后直接跳到 v0.5.19——v0.5.1–v0.5.18 的条目在本仓库中不存在（无记录，非漏写）。

---

## v0.6.4

> 注：本版开发过程中曾以 0.6.3 为工作版本号，发布时统一为 0.6.4——v0.6.3 未单独发版，
> 其「译文扩页」与本版同批发布；本版审查报告见 `docs/代码审查-v0.6.4*.md`。

**「译文扩页」成为界面可选项 + 版面重叠三处根因修复 + 全局译文字号缩放**（本版全量 **877 全绿**）。扩页是发布前评估里的
「取消原/译页数一一对应」的最小落地切片：**不做**全局取消对应，而是加一个默认关闭的
导出旋钮，让装不下的内容改排到新增的后续页。

① **动机**：固定在「源页 i → 输出页 i」的框架下，装不下时只有两条有损出路——
`_compute_table_layout` 的**页底钳制**把整表行高等比压缩（日志「表格译文超出页底，已压缩
行高至 N%」），或把字号降到可读下限（正文 7pt / 表格 6pt，扫描件可低到 3pt）。
② **实现**（`translate_app/pdfio.py`）：新增 `save_translated_pdf(..., expand_pages=False)`。
开启时**不传 `page_height`**（关闭页底钳制），`_compute_table_layout` 新增 `return_rows=True`
返回**逐行几何**（`grid` 的线段不带行身份，无法按行拆分；默认 `False`，旧调用与测试不受影响）。
随后 `_flow_elements` 把页面排成「表格行 / 正文块」两类元素，`_flow_split` 在**第一个装不下的
元素**处切分（表格下推后内容按 y 单调，故「从那里往下全部搬走」成立），`_flow_grid` 只用留下的
行重建首页网格（没搬走任何行时与钳制版的 `grid` 逐字相同）。搬走的部分由 `_draw_overflow_pages`
在**新空白页**（同尺寸、同 `/Rotate`）上从页顶重新流排：**行不跨页拆开**、续页**重复表头行**
（`_flow_header_rows`：连续的表头行、映射块全非数字、最多 2 行，空行按数据行处理以免把全文字列
当表头反复重画）、正文保留源间距（钳在 0–18pt）。
③ **边界（有意为之）**：只对**文本层**内容生效。扫描件（位图表格线、印章、手写签字）与图内文字
钉死在像素上、不可重排——`_flow_skips` 把 `is_chart` / `in_image` / 保留块排除在移动之外，
`ocr_table` 路径不扩页（扫描表格本就不做行高扩展），`rebuild_table` 的 OCR 重绘路径保持原逻辑。
**双语产物不适用**（`worker._export` 会记一行提示）：交错产物靠 `src[i] ↔ tgt[2i+1]` 配对，
扩页会打破它，`check_translation.py` / `check_layout.py` / `main_window._translation_output_page`
都按该配对工作。
④ **接线**：`TranslateWorker(expand_pages=…)`（默认关，`PDFTRANSLATE_EXPAND_PAGES=1` 强制开），
`_options_line()` 增加「译文扩页=开/关」；`MainWindow` 的选项区变三行，新增「译文扩页」勾选框
（`prefs.json` 键 `expand_pages`，`_persist_expand_pages` 与其它勾选框同款即时持久化），
两处 `TranslateWorker(...)`（开始翻译、重新导出）都传值。扩页是**导出期**选项，不改提取结果，
所以「重新导出」无需重新提取。
⑤ **回归 +6**：`ExpandPagesTest` 4 例（默认仍一页且照旧钳制 / 溢出行排到续页并重复表头且重画网格 /
装得下时一页都不加 / 续页行序正确且无内容出页）、`test_worker` 2 例（选项到达导出器且出现在选项
日志 / 默认关且受环境变量门控）。真机合成表实测：同一份窄列表格，默认输出 1 页且记「已压缩行高至
31%」，开启后输出 2 页、续页重复表头、行高不再压缩。
⑥ **审查修复（本版代码审查 P1/P2）**：
- 扩页**不再搬运任何 `ocr` 块**（`_flow_skips` 对 `ocr` 一律返回 True）——扫描块的文字是位图，
  搬走会让首页原文裸露、续页多出一份译文（首页白底覆盖只对「留在原页」的块生效）。
- `_flow_draw_row` 对缺失译文的单元格（较短的 `per_page`，函数本就声明容忍）直接跳过，
  不再 `IndexError`。
- `save_translated_pdf` 返回**源页 → 输出页映射**；`TranslateWorker._page_map` 记录、
  `MainWindow` 用它定位预览的「译文」页——扩页后源页 i 不再等于输出页 i，此前预览会错页。
  回归 +4（`ExpandPagesTest` 3 例、`test_ui` 1 例）、`test_worker` 增补映射断言；全量 **862 → 866 全绿**。

⑦ **本版追加 A —— 版面重叠的三处根因修复**（真机样本「Evergrowning Bank - Top 10 Shareholders」，
`check_layout.py`：重叠 29 → 3、漏画 1 → 0、压线 1 → 0）：
- **双栏阅读顺序**：`_order_generic` 的「全宽元素」判据（`宽度 > 1.5 × 中位宽`）在窄单元格
  （表格格宽 5–60pt）把中位宽拉低时，会把**两栏的正文行**判成全宽；这些行被抽出后按 y 重排，
  两栏交错，一个 9 行段落被切成 6 个 2 行小块、各自从自己的顶对齐，更长的英文逐块压到下一块上。
  现在要求全宽项**同时**跨越页面文本宽度的 75%（`_FULL_WIDTH_SPAN_RATIO`）。
- **只识别出表头的表格**：`find_tables` 要求两向都有框线——报表常只画表头带（列分隔线 + 表头下的
  横线），正文行没有竖线，于是整张表只返回一行表头，数据行按散文排版、在 32pt 窄框里折成 4 行压住
  下一行。新增 `_extend_with_body`：用表头列边界 + 表格收尾横线（同一 y 上的分段先合并成一个跨距）
  向**上下两侧**补齐表体行（界内横线分带；无内线时按行中心等分并按行距收敛），合成行标
  `borderless`（源本无线，不重画网格）。数据格于是拿到整列宽度走单行拟合，行高扩展也恢复生效。
- **图内文字 OCR 与文本层重复**：`image_text` 把横幅图里的标题又 OCR 了一遍，OCR 框（整条横幅）比
  文本层字形框大 7 倍，单向覆盖率 14% < 50%，去重失败 → 同一标题被画两遍（100% 重叠）。
  `_merge_ocr_blocks` 增加**对称包含**判据（文字完全相同且文本层块被 OCR 框覆盖 ≥90% 也算重复）。
  回归 +5：`ParagraphGroupingTest` 1 例（窄单元格不得让列行变成全宽）、`HeaderOnlyTableBodyTest`
  2 例（表体成格 / 整宽段落终结表体）、`OcrDuplicateContainmentTest` 2 例（重复块被丢 / 含额外文字
  的超集块不被误丢）。

⑧ **本版追加 B —— 全局译文字号缩放**（同一样本：压线 1 → 0）：
① **动机**：西文在同号下比汉字「重」（汉字只占 em 框的一部分），译文按源字号画出来整体偏大。
② **实现**：`_TRANSLATION_FONT_SCALE = 0.9`（`translate_app/pdfio.py`），经 `_font_start(block)`
同时作用于**拟合起点**与两个可读下限（`_MIN_READABLE` 7.0 → 6.3、`_MIN_TABLE_READABLE` 6.0 → 5.4；
`_MIN_TABLE_FLOOR` 仍是绝对 3.0）。所有页面类型都经 `_fit_block`，因此文本层页、**OCR 页**
（整页扫描 / 稀疏文本层 / `_draw_ocr_grid_page` 重建扫描表 / `_draw_ai_table` 矢量重建表）与图内
文字一并生效；审计侧（`agent/flow.py` 的 `check_layout`、`eval.py`）改用同一个 `_font_start`，
保证「审出来的字号 = 画出来的字号」。可用 `PDFTRANSLATE_FONT_SCALE` 覆盖（越界/非法值回落 0.9），
`1.0` = 旧行为。
③ **验证**：真机样本 9 → 7.2、8 → 6.1–7.2（表格正文不再显得比原文大）；扫描样本
`test_mintai_sample.pdf` 在 1.0 / 0.9 各导出一次对照，OCR 表格更紧凑、行距贴合源表；回归 +2
（`TranslationFontScaleTest`：起点与下限随缩放、渲染字号严格小于源字号），并把两处写死阈值的旧
测试改为**跟随缩放常量**（仍能抓住「标签被压到 4.5pt」的老 bug）。**全量 877 全绿**。

---

## v0.6.2

**「翻什么 / 不翻什么」由模型判定（第 1、2 步）**（用例 822 → **856 全绿**）。
两步都把「不经模型、硬性生效」的确定性决策降级为**默认值**，模型只在其上做判断；
每一步都规定了失败方向，且**红线一律不进候选、也不可被答案触碰**。
① **页范围（第 1 步）**：`run_translate` 改由模型读用户要求里的页范围——
`agent.ai_scope(requirement, llm)` 复用 `make_llm_flow_compiler` 的槽填充（JSON 的 `scope` +
可选 `reason`），日志回显「AI 理解为只翻第 N 页（reason）」；任意解析失败 **fail-open 到整篇**
（静默少翻比多翻更糟）。`parse_explicit_scope` 退为**无模型时的离线兜底**。
② **内容策略（第 2 步，新增 `translate_app/policy.py`）**：第 1 步只让 **agent** 路径能逐块覆盖
图表默认，**IR 管线与确定性回退路径上没有任何 AI 决策点**；现在 `TranslateWorker._content_policy`
在两条路径上都先问一次模型，候选只有两类——`role == "figure"` 且非 `in_image`（默认保留，可
**放行**翻译）、`ocr` 且非 `in_table` 且未保留（默认翻译，可**保留**原文，对应扫描印章/公章/
手迹/二维码）；提示词 `prompts.content_policy_task`（每页 ≤6、全篇 ≤120，未列出的一律沿用默认）
回 `{"release": [...], "keep": [...], "reason": ...}`，`parse_policy_json` 只接受**候选集内**的
整数索引；keep 写成 `Block.keep_original`（导出/审计/prose 分组共用的唯一通道），release 交给
`translate_ir(release=…)`。**红线块从不进入候选**（数字/金额、公式、表格单元格、`is_chart`），
且 `ir._keeps_source` 把红线判断放在 release **之前**——即使答案里写了公式或金额的索引，
它们照样保留原文。默认开启，`PDFTRANSLATE_CONTENT_POLICY=0` 可关闭；**失败一律 fail-open 到
确定性默认**（无模型/网络错/坏 JSON/回调抛异常 → 空决策，两条路径与改动前逐字一致）。
③ 回归 **+29**：新增 `tests/test_policy.py`（21 例：候选筛选/红线排除/上限/JSON 解析/apply/
LLM 槽与失败路径）、`test_ir.py` +4（figure 默认保留、release 后照常翻译、release 放不动红线、
`keep_original` 不送模型也不并入段落）、`test_worker.py` +4（两条路径接线、失败 fail-open、环境开关）。

## v0.6.1

**组织架构图/架构图的「保留原文」改为可被 AI 逐块覆盖**（用例 818 → **822 全绿**）。
v0.6.0 的「chart 页 OCR 块一律不覆盖不重画」是硬规则，现在降为**默认策略**：
① 导出端只跳过**未被翻译**的 chart 页 OCR 块（译文==原文），翻译过的块照常绘出；
② `page_task` 的 `kind == "chart"` 分支明确告诉模型「图内标签默认保留原文、只译图外文本；
本轮用户**明确**要求时才用 `translate_block`/`translate_blocks` 覆盖」；
③ `DocumentSession._preprocess` 把图表页 OCR 块标 `keep_original`（导出保留原像素、审计
`_audit_protected` 不当漏译），**写译文**（`_write`）清除该标记、**`delete_block`/标注删除**
重新设置该标记——AI 由此可按内容逐块决定翻不翻；
④ **红线不变**：数字格、照片/彩色底、签字/印章、公式、丢页、控制信号仍不可被 AI 触碰。
回归 +4：`KeptOcrPixelsTest`（默认 + 逐块覆盖）、`test_page_task_keeps_a_chart_diagram_by_default`、
`test_write_clears_keep_original_and_delete_sets_it`、`test_preprocess_keeps_a_scanned_diagrams_labels`。

## v0.6.0

**代码审查修复批次 + 组织架构图/架构图改为保留原图**（用例 807 → **818 全绿**）。
① **体检工具可信度**：`check_translation` 对双语产物改按「源页 i ↔ 译文页 2i+1」配对（此前拿原文页
当译文页比对，译文页里数字被改错也判「体检通过」）；`check_layout` 的源文本层提取失败改为
fail-closed（不再把每页当扫描页、静默跳过漏画检查后打印「体检通过」）；`--lang zh`/`cn` 识别为
CJK 目标；缺文件统一 exit 2 而不是 traceback。
② **控制信号**：`ChatCancelled` 继承 `ControlSignal`，回复末段的 `except Exception` 不再吞掉取消。
③ **页范围提示词**：补 `保留/留/想要/需要/想`——此前「只保留第3页」会被静默放宽成整篇翻译。
④ **图内文字**：补上设计文档要求的 `char_count ≥ 4` 保守门槛（1–3 字符 OCR 碎片不再覆盖图片）；
图区判据统一为 `_image_regions_for_ocr`（>50% 页的大图不再出现纯白覆盖疤、双语译文页不再丢图）。
⑤ **组织架构图 / 架构图保留原图**：`classify_page == chart` 的页（≥3 个窄高节点框）导出时**既不覆盖
也不重画**其 OCR 块——修复真机年报第 5 页 **7 处文字重叠**（重画的识别框横向压到邻格，最大 73%）
与 5.0–5.6pt 小字号；图内 88 个 OCR 块保留原始像素，页眉/大标题照常翻译。
⑥ 已知未修：`check_layout` 对「表格大幅下推」的块仍可能假红（已复现定位：绝对坐标匹配在位移下
会误判归属，需先测量源页↔译文页的规则线位移），留待后续单独处理。

## v0.5.47

**移除界面「表格列宽重排」选项，该能力改为始终生效**（默认开）。
文本层表格的列宽按译文需求重分配（长译文列借用相邻列空白、数字列不缩、表格总宽不变）
此前是默认关的复选框，用户不勾就享受不到；现在 `TranslateWorker` 的 `reflow` 默认 `True`，
「开始翻译」与「重新导出」都自动应用，不再依赖某个勾选框。选项区因此从三行收成两行
（`翻译管线/术语注入`、`扫描重建/图内文字`）。需要排查/对照旧行为时可用
`PDFTRANSLATE_REFLOW=0` 强制关闭（与 `PDFTRANSLATE_AGENT_TERMS=0` 同款）。
仅影响「仅译文/原位」PDF 的文本层（矢量线）表格；扫描件位图表格线与双语 PDF 不受影响。

## v0.5.46

**「翻译图内文字」成为界面可选项**（默认勾选，用例 804 → **807 全绿**）。
位置：「保存到」下方选项区第三行（`翻译管线/术语注入`、`重排版/扫描重建`、`图内文字`），
勾选框文案「翻译图内文字」。**勾选**＝v0.5.44/v0.5.45 的行为（混合页里烧在位图上的文字：
OCR → 按图片背景色覆盖 → 原位画译文；照片/彩色底整图不动、图内数字刻度不译、旋转页跳过）；
**取消**＝图内文字保持原样（与 v0.5.43 及更早一致）。开关即时写入 `prefs.json`
（键 `image_text`，默认 `true`）并在下次启动恢复。**IR 模式同样生效**：
`extract_document_structured` / `extract_structured` 现已透传该参数——此前结构模式会忽略它
（`run.bat` 默认 `PDFTRANSLATE_IR_MODE=1`，所以这条不补就等于在默认启动方式下开关无效）。
**「重新导出」的一致性**：图内块是**提取期**决定的，复用上次的 `DocumentText` 会把当时的
设置一起带过来；新增 `DocumentText.image_text` 记录该值，`TranslateWorker._run_re_export`
比对不一致就**重新提取**（否则刚勾上开关却仍输出未译的图，或取消后又多出图内块）。
回归 +3：勾选框存在且默认开、状态确实传给重新导出的 worker、设置变更时重新提取；
`test_the_option_can_turn_figure_text_off` 覆盖「`ocr=True` 但 `image_text=False`」在
**直连与结构两条路径**都不产图内块。

## v0.5.45

修 **v0.5.44 图内文字在「IR 模式 + DocLayout」下被语义层保护成原文**（用例
802 → **804 全绿**）。症状：按 `run.bat` 启动（`PDFTRANSLATE_IR_MODE=1`，且 venv 里装了
`doclayout_yolo`）时，图内文字**被识别、被覆盖、被重画，但画的还是原文英文**——真机
`test_1.pdf` 图区 334 个拉丁字符、**0 个汉字**；而直连管线（无 figure 区域）同一文件
译出 72 个汉字。根因是**两个特性相撞**：DocLayout 检出 figure 区域后，
`build_structure` 把落在该区域**框内的所有块**收进 `block_indices`（`pdfio.py:389`），
其中包含我们刚从图里 OCR 出来的文字块 → `ir._role_of` 给它们 `role="figure"` →
`ir.translate_ir` 的「结构角色保留原文」门槛（`_STRUCTURAL_ROLES = ("formula","figure")`）
把它们原样保留。**修法**：① `build_structure` 里 figure/formula 区域**不再认领
`Block.in_image` 块**（图区 bbox 仍覆盖图片本体，图片照样受保护）；② `translate_ir` 的
判定处加同一守卫作 fail-safe。**验证**：同一解释器与配置（`C:\pv` + doclayout + IR），
修复前图区 0 汉字 → 修复后 72 汉字（「温度缩放保持词元排名 / 相同 / 胜出者 / 概率 /
词元 A /（重复性）…」）。新增回归 2 条（`FigureTextRoleTest`：图区不认领图内文字块、
IR 门槛仍翻译 `in_image` 块）。

## v0.5.44

**混合页（文本+图片）里的图内文字现在会被翻译**（用例 798 → **802 全绿**）。
起因是一份 5 页论文样本：第 2 页的柱状图在中文产物里**逐像素未变**，图上的英文标题、
轴标签、图例全是原样。根因在 `extract_document_text` 的触发条件（`pdfio.py:980`）——
页 OCR 只在 `not lines or sparse_text`（无文本层或 ≤3 行）时跑，**有完整文本层的页面
从不 OCR**，于是烧在位图里的文字既不在文本层、也从没被识别。
两条路都由上述机制兜住：① **图区枚举 + 图区 OCR**（`_image_regions_for_ocr` /
`_region_to_array` / `_image_text_blocks`）——只对**有完整文本层**的页跑，只 OCR 图区
（`get_pixmap(clip=…)`，不重扫整页），逐条框校验/数字归一化与扫描页共用同一实现
（新抽出的 `_ocr_items_from_results`），产物是带 `in_image=True` 的普通块，因此翻译缓存、
对话覆盖层、`set_text`/`retranslate`、审计全部**自动可用**；② **导出按图覆盖**
（`_cover_image_text`）——覆盖矩形取**该块自己的 OCR 框并裁到图片范围内**，填充色从
**图片自身外边距**采样（`_image_cover_color`），**不用**扫描页那条「实测字形带」路径：
柱状图的柱子是大块深色游程，会被判成墨迹一起擦掉（实测柱子像素 100% 保留）。
安全门：图区整体判为**非纸面**（照片/彩色底）时**完全不建块、不覆盖**，图内文字保留原样
并记日志；图内**纯数字/刻度**（`0.684`、`0.400`、轴刻度）不建块（语言中立，覆盖后重画
同一串数字只有风险）；图内块**永不进表格网格重建**（`in_table` 恒 False，否则图里的数字
会被当成报表重排）；换行由**行带 `fit_height` + 宽度预算 `fit_width`** 约束（`_figure_block_bands`），
第二行不再压到图里下一行文字（实测：坐标轴标题曾被刻度标签的第二行压住）；
**旋转页暂不处理**（`clip` 与 `/Rotate` 的先后顺序未实测，宁可跳过并记日志，也不冒
v0.5.39 那类采样帧错位的风险）。方案与分阶段计划见 `docs/0.5.44-混合页图片翻译方案.md`。

## v0.5.43

修**修复批次（v0.5.38–v0.5.42）自身**的三处缺陷（新增 3 条回归，
用例 795 → **798 全绿**）：
① **`check_layout` 假绿**（上一步 S3/P0-4 的副作用）：`_covers` 的「同列且在半块高以内」
这条宽松判据**不区分 span 归属**，于是*相邻*块的译文替漏译的块作答——一个 12 行段落整段
没画也判「体检通过」（exit 0）。现在每条译文字形带只归**一个**源块（`_credit_key`：先看
「框是否含该带中心」，再比垂直距离，并列取靠前的块），漏译宁可报出（fail-closed）。
② **取消对话会毁掉会话**（上一步 P1-23 的副作用）：`reply` 在工具循环中途抛
`ChatCancelled` 时，assistant 消息已声明**全部** `tool_calls`，剩下那些却没有对应的 `tool`
回复 → 下一次请求会被服务端按工具协议拒绝（会话就此损坏）。现在取消时先给不会执行的
tool_call 补一条 `tool` 回复（「已取消」）再上抛。
③ **未配对的目标页不检查**：`check_document` 的循环只遍历*源*页，目标更长时多出来的页
从不检查、却仍可能打印「体检通过」；现在非双语产物里没有源页对应的目标页也会查
重叠/出页/字号，并在 `[页数]` 里点名（双语交错产物不受影响）。
另：修正 `pdfio._draw_ocr_grid_page` 的页底钳制与 `check_layout.to_render_frame` 注释里
对坐标帧的陈述（两处**代码本身正确**：钳制取的是被绘制的那张空白页自己的底边；渲染帧与
`get_text` 帧都以 cropbox 左上角为原点，已实测）。

## v0.5.42

按 `docs/代码审查-v0.5.37.md` §9 第 6 步修**对话 / agent 门禁类**
（测试 789 → **795 全绿**）：
① **页范围误判**（P1-15）：`parse_explicit_scope` 的提示词里含**裸「只/仅」**，
于是「只要第5页的数字没错」「不仅第3页」被当成范围限制、整篇只翻一页；现在只认
限制短语（`只翻/只查/只看/仅翻译…`、`仅限/限定/范围`、或紧贴「第N页」的只/仅，
且 `不`+只/仅 一律不算）。
② **自检 fail-open**（P1-16）：`_ai_self_check` 的 `audit.get("clean", True)` 把
「审计没返回结果」读成「复核通过」；改为 `is True`，缺结果时记「自检未返回结果」。
③ **`set_text` 写入字面量 "None"**（P1-22）：`str(None)` 会写进受保护覆盖层并在导出时
生效；现在非字符串/空串一律拒绝（`apply_annotation` 同步收紧）。
④ **取消不能中止工具循环**（P1-23）：看门狗只能中断「阻塞中的请求」，取消若发生在工具
执行期间，循环会跑完剩余轮次（实测又执行 7 个工具 + 8 次模型调用）；`ChatSession.reply`
新增 `cancel` 回调，每轮、每个工具执行前检查，命中即抛 `ChatCancelled`。
⑤ **`retranslate` 静默改写别页**（P1-24）：页内序号被当扁平索引 → 写到了**另一页**
且报 ok=True；现在按页归属过滤，全越界时返回该页的合法索引范围。
⑥ **`run_plan` 的门禁**（P1-25/P1-26）：审计任务默认 `auto_fix` 从 True 改为 **False**
（与 `run_flow` 一致、与工具描述一致）；`scope` 强制整型化 + 范围校验（字符串 `"3-8"`
曾在运行中抛 TypeError，字符串列表会「零翻译但成功」）。

## v0.5.41

按 `docs/代码审查-v0.5.37.md` §9 第 5 步修**「重新导出」路径**
（测试 786 → **789 全绿**）：
① **复用上次运行对齐用的 `DocumentText`**（新增 `TranslateWorker(last_doc=…)`，
`MainWindow` 在运行成功时从 worker 取回）：此前「重新导出」每次直接调
`extract_document_text`——注释写「缓存，便宜」，实则生产无 OCR 缓存，**扫描件整篇重 OCR**
（实测 7–13s/页），还绕过 `structure_mode` 与 `PDFTRANSLATE_OCR_BACKEND`（VLM 后端跑出来的
译文会用 RapidOCR 重导）。现在**不再重复提取**。
② **无上次文档时改走 `_extract_doc()`**（与首次运行同一路径），而不是直调纯文本提取器。
③ **配对前校验块数**：不一致时提示「源文件或提取结果已变（块数 N ≠ 上次 M），上一次的译文
不再适用，请点「开始翻译」重新翻译」，不再抛 `block_pages … must be parallel lists` 这类
技术性异常整轮失败。

## v0.5.40

按 `docs/代码审查-v0.5.37.md` §9 第 4 步修**扫描表格的判据与行带**
（测试 782 → **786 全绿**；**`_OCR_CACHE_VERSION` 8 → 9**）：
① **无千分位的数字也认**（P1-6）：`_NUMERIC_CELL_RE` 增加「无分组」分支，`1000`、
`1234.56`、`2025` 现在算数字格——此前整列这样的值会让 `_is_numeric_column` 失败，
整页被当散文重建（没有行带、没有右对齐、整框白底）。
② **注记列不再让散文页变成表格**（P1-7）：`_is_numeric_cell` 排除 `(1)`/`（二）`/`八)`
这类附注标记，`_is_numeric_column` 还要求列内至少有一个「强数字」（含分隔符/小数/
百分号或 4 位以上）——此前双栏扫描**散文页**只要有一列 `(1)(2)…`（或裸 `1..8`）就
被判成表格，勾选「OCR表格重建」后整页重绘、**扫描底图丢失**。
③ **稀疏页并入不再重复**（P1-8）：`_merge_ocr_blocks` 的文本判据由「完全相等」改为
「互相包含」——文本层把两行标题并成一块、OCR 逐行返回时，标题此前被加了两遍
（Markdown/纯文本重复、PDF 两块重叠）。
④ **行带恢复为真实行距**（P1-5）：`fit_height = 下一行行顶 − y0 − 1.5`（v0.5.36 曾把它
抬到字形高度，等于放宽了**换行预算**，第二行会压进下一行）；「高标签旁矮数字格」的
可读性改在 `_fit_block` 里解决——**单行**可用字形高度（实测同一格 3.0 → 6.2pt），
绝不换行越过表格线。因语义变化递增 `_OCR_CACHE_VERSION` 到 9（旧缓存里的行带是抬高的）。

## v0.5.39

按 `docs/代码审查-v0.5.37.md` §9 第 3 步修**同一类「坐标帧」缺陷**
（旋转页 / CropBox），测试 777 → **782 全绿**：
① **旋转页照片不再被白底挖洞**：`_photo_ocr_blocks` 先把块框经 `_rot_map_rect`
映射到 `get_pixmap` 的**渲染帧**再判「纸 / 照片」——此前直接拿未旋转框采样，旋转页
上取到的是转置后的位置（实测读到白纸），于是白底扣在照片上；四种旋转逐例回归。
② **`_rot_map_rect` 改用 `page.cropbox` 尺寸**（原用 `mediabox`）：CropBox 裁剪过的
扫描件（原点非 0）此前整体偏移（实测 rot=90 + CropBox(50,100,562,742) 偏 50pt），
白底/字形带采样随之取错位置；现在 4 种旋转 × 有无裁剪共 8 例与渲染像素对拍一致。
③ **表格页底钳制传 `page.cropbox.height`**（原 `page.cropbox.y1`）：块坐标是
cropbox 相对坐标，`.y1` 含裁剪偏移 → 钳制永不触发、页底表格被画出页面（静默丢内容）。
④ **`_draw_ocr_grid_page` 行扩展加页底钳制**：扫描表格重绘从空白页开始，行被推出
页底即无痕丢失；现在按比例压缩行高并记日志（回归 `test_ocr_grid_redraw_stays_on_the_page`）。
⑤ 顺带修 **`_draw_vertical_label` 的页内钳制**：改用未旋转 cropbox 帧——此前在
`/Rotate 90` 页上把 y=300..390 的竖排标签推到 y≈160（错位 187pt）。

## v0.5.38

按 `docs/代码审查-v0.5.37.md` 的 §9 修**第 1、2 步**——2 个产品 P0
与体检工具的 8 条 P0（测试 763 → **777，全绿**）：
① **表格换行不再拆断金额**（`_split_line_half` 补 `_is_amount_atom`，与 `_break_word`
同一判据）：`US$1,234,567.89` 曾被「凑行数」的再平衡切成 `US$1,23` + `4,567.89`。
② **OCR 框坐标校验**（`_synthesize_ocr_blocks`）：NaN/inf 坐标曾一路进 `Block`、
导出时抛 `ValueError: cannot convert float NaN to integer`（整轮失败）；畸形/扁平框
曾抛 `TypeError` 让**整页** OCR 结果被丢弃。现在逐条跳过并记日志。
③ **`check_layout.py` 旋转页帧**：`get_text` 坐标是未旋转帧、判界与采样却用
`page.rect`/旋转渲染帧 → 正确产物报「越出页面」exit 1，且「压线」在旋转页恒 0 条。
现在判界用未旋转 cropbox 帧、采样前把字形带映射到渲染帧（四种旋转 + CropBox 偏移
逐例对拍像素验证）。
④ **`check_layout.py` 漏画判据**：原来要求**单个 span 覆盖整块面积的 10%**，12 行段落
的短译文只盖一行（≈8%）→ 必然误报；现在按「span 是否落在块内（或落在被表格下推后的
位置附近）」判断，长段落不再误报、整块丢掉仍然报。
⑤ **双语产物按「源页 i ↔ 译文页 2i+1」配对**（原来 `tgt[i]` vs `src[i]`，正确产物假红、
目标里的源页恒假绿）。
⑥ **`--page` 越界/非法 → exit 2**（原来越界页静默无检查、还打印「体检通过」）。
⑦ **`check_translation.py` 数字按值比较**：分隔符角色相同就 `return []` 的短路让
「`1,234.56 万元` vs `1,234.56 yuan`」（丢万倍率）判「一致」；`%` 未剥离导致百分比
变化 10× 全绿；会计括号负号 `（1,234.56）` 与正数等价。三者都修，且正确换算
（`3.14 亿元` ↔ `314 million yuan`）仍不报。
⑧ **扫描页的残留中文检查不再被整页早退跳过**（真机样例第 2 页 750 个残留汉字此前未报）。
回归：`tests/test_pdfio.py`（+5）、`tests/test_check_layout.py`（+5，且**首次真正跑导出产物**）、
`tests/test_check_translation.py`（+4）。

## v0.5.37

把上一轮临时写的「版面体检」固化成可复用的脚本 **`check_layout.py`**
（与 `check_translation.py` 互补：那个查**内容**，这个查**排版**；两者都只看导出产物、不需要模型）：
```
python check_layout.py 原文.pdf 译文.pdf [--page 3,5-7] [--dpi 150]
```
检查项与判据（全部与文档无关）：
① **译文重叠**——两个译文的**字形带**（按字体上升/下降部算，不是松散 bbox）相交面积 ≥
较小者的 25%；
② **超出页面**——字形带越过页面左右下边界；
③ **压线**——字形带**内侧**（上下各缩 20%）落在源页印刷表格线上的比例 > 5%；
但**若源页同一位置的文字本来就被那条线穿过**（扫描报表的虚线常常这样），则不算缺陷；
④ **字号过小**——低于表格下限 6pt / 正文下限 7pt（逐条列出，超过 20 条汇总）；
⑤ **漏画**——源页有文本层的块，译文里没有对应位置的文字（扫描页跳过并提示）；
⑥ **页数 / 页尺寸**不一致。
退出码：0 = 无结构性问题（字号偏小只告警）；1 = 有重叠/出页/压线/漏画；2 = 用法错误。
真机自测（`test.pdf` → `test_English.pdf`）：**无结构性排版问题**，282 段字号偏小（扫描密集
报表的物理极限，已汇总提示）；用它造了 4 类故意坏样本，逐项都能抓到。
回归测试 `tests/test_check_layout.py`（15 个，纯函数 + 端到端）。

## v0.5.36

用一套**与文档无关的版面体检**（重叠 / 出页 / 压线 / 小于可读下限 / 源墨迹残留）
扫了真机 `test.pdf`→`test_English.pdf`，找出两个**有代表性**的排版缺陷并修掉：
① **译文整体被下移**：v0.5.34/v0.5.35 把「下一条表格线」当成**把框拉长到那条线**，而单行译文
是在框内**垂直居中**的——框一拉长，译文就整体下沉（真机实测同一行可下沉 **+10pt**，扫描件标题
「利润表」下沉 13.6pt 并压住下一行的副标题）。现改为：**框不动，只把这条线当作高度预算**
（`_fit_block(avoid_below=…)` 用它限制字号/换行），译文回到源行位置。
② **表格行带比单元格自身还矮**：OCR 网格的 `fit_height` 按「下一行文字顶部」算，遇到
「高标签旁边是矮数字格」的行时，下一行顶部落进本格里 → 行带只有 **3.5pt**（本格高 8.1pt），
译文被压到 **3pt**。现 `fit_height` 下限取**单元格自身字形高度**（源文本本来就装得下），
同一格从 3.0pt 提升到 **4.3pt**；并**递增 `_OCR_CACHE_VERSION` → 8**（缓存里存的就是这些
几何量，不递增则已 OCR 过的文档不会重算）。
体检结果（修后）：两页 **0 处重叠、0 处出页、0 处压线**；第 1 页无 <7pt 文字；
第 2 页逐格与源行的垂直偏差中位数 **0.32pt**（p90 0.37pt）；表格线被擦像素与修前持平。
新增回归 `test_rule_bound_does_not_move_a_single_line_translation`，
并更新 `test_tight_rows_keep_a_band_and_stay_inside_it`（行带下限）。
**仍存在的物理限制**：扫描密集报表里「窄列 × 长英文标签」的格子（源行高 ~8pt）只能排到
3–5pt——想要可读的表格请勾选「OCR表格重建为矢量表格」（行高按译文重排）。

## v0.5.35

把 v0.5.33/v0.5.34 的白底与避线逻辑**改成与文档无关的通用做法**——原先的
阈值与判定是按 `test.pdf` 的对比度、尺寸、朝向调出来的，换一份扫描件（更淡/更暗/旋转/窄表格）
就会失准。现在全部自适应：
① **亮度阈值按页面自身算**：用**局部纸面**（16pt 网格内的 90 分位，再取邻域最小值——
白页边距不能抬高紧邻灰扫描区的纸面基准）+ 页面稳健对比度（p90−p1）推出
「印刷 / 字形核心 / 抗锯齿边」三个偏移量，清洁扫描（250/30）、灰复印件（200/90）、
淡传真（235/165）都无需调参；
② **表格线判定按点（pt）而非像素**：长 ≥10pt、厚 ≤3pt、且自身有 ≥3pt 实线（点状纹理是
1–2px 点，被这条挡掉）；虚线按 2pt 闭合；
③ **表格线掩膜对每一页都算**（不再只在有 OCR 块的页上算）——所以**文本层页面**上
「标题/单位说明下方压着一条下划线」也会被避让（不再只认 `find_tables` 检出的表格）；
④ **旋转页（/Rotate 90/180/270）现在真的能测量**：把块框映射到渲染帧采样、白底矩形再映射
回来，并按「细长竖条」识别旋转后的表格线（此前旋转页一律回退整框白底、会压线）；
⑤ **照片/纸判定改为局部**：区块自己的纸面亮度必须主导该区、且不低于页面纸面的 75%，
灰纸扫描仍算纸、均匀深色照片不再被误判为纸；
⑥ 右对齐长行横向溢出时仍钳在页内。
新增 4 个通用性回归：淡扫描仍紧贴白底、深色扫描的纸面不被当成墨、旋转扫描不压线、
下划线（非表格）上方标签不换行。真机复测（同一份 2 页报告）与 v0.5.34 一致：
残影无、表格线被擦像素 315（独立度量，与 314 持平）、单位标签 1 行不压线。

## v0.5.34

用真机 `test.pdf` / `test_English.pdf`（2 页：一页文本层报表 + 一页扫描利润表）
逐处对照，修掉 3 个**可见**缺陷：
① **单位标签换行压住表格上框线**：`单位：人民币万元` → "Unit: RMB in ten thousand yuan"
在源框里放不下，被折成两行、第二行正好压在表格上框线上（两张表都中招）。现
`_fit_block` 新增 `avoid_below`（该块下方最近的表格上框线）：**单行源块**若换行会越线，
就保持**一行**、降到可读下限、向旁边的空白横向溢出（横向溢出可恢复，压线不可恢复）；
右对齐的行改为向左溢出而不是被夹到框左边。
② **白底残影**：扫描页的字形笔画比「深色核心」浅，白底只盖住了核心，淡笔画留在译文底下
（放大可见）。现字形带 = 深色核心（`_INK_LUMA`=140）**再向上下各生长 ≤9px 的印刷行**
（`_INK_EDGE_LUMA`=200，绝不进入表格线行），淡笔画也被盖住。
③ **译文压行线**：OCR 行带是「下一行文字顶部 − 本格顶部」推出来的，**包含**那一行线，
于是换行后的第二行会穿过虚线。现新增**页面级表格线掩膜**（长且细的印刷游程：
`_RULE_MIN_RUN_PX`=30px、厚度 ≤6px、且自身有一段 ≥6px 的实线——扫描纸面的点状纹理是 1–2px 点、
会被这条挡掉；虚线先用 `_RULE_GAP_PX`=4 闭合成一条线），据此：白底按「每个文本行 × 避开竖线的 x 段」
逐块绘制、多行框得到多块；译文的可用高度被**下一条表格线**截断（`_rule_below`）。
真机复测（同一页扫描利润表）：白底残影消失；表格线被擦掉的像素 417 → **173**（0.20%，
且剩下的大多是印章/表号区，不是表格线）；单位标签从 2 行 → **1 行**且不再压线。
回归：`test_single_line_label_above_a_table_stays_one_line`、`test_faint_scan_text_is_covered_too`、
以及 v0.5.33 的 3 个白底测试（已按新语义更新）。
**仍未处理（不影响压线）**：`二、补充财务数据` 这类标题仍可能折成两行（它下方是单位标签、不是表格线）。

## v0.5.33

**未勾选「OCR表格重建为矢量表格」时，扫描页的白底不再压线**——白底形状与译文
改为贴合**实测字形带**。原先白底取整个 OCR 框（RapidOCR 的框上下带留白、在扫描报表上常常
一直顶到表格线），于是白底把位图表格线一起擦掉（用户截图中虚线被白框切断）。
现按 150 dpi 渲染该页**采样像素**测量：
① 一行/一列像素若「几乎整行都被印上」（`_SOLID_LUMA`/`_RULE_LINE_SHARE`）即判为**表格线**，
它**终止字形带**且**不进入白底范围**——每个文本行各自得到一个贴合的白底矩形，
行与行之间的表格线、以及单元格左侧/右侧的竖线都完整保留（框贴到竖线时横向也自动避开）；
② 多行 OCR 框（合并的标签碎片）会得到**多个**白底矩形，不再是一整块；
③ 白底只覆盖真正有墨迹的行，`luma` 阈值与「至少 2 个像素」的门限滤掉扫描噪点；
④ 段落型 OCR 块的**译文**也画进这条带内（表格单元仍用行带 `fit_height`，避免换行译文越到下一行）；
⑤ 旋转页 / 无 numpy / 测不到墨迹时**保持原来的整框白底**（安全回退）。
实测（合成扫描报表，OCR 框=整行高、上下顶线）：白底从 30pt 高收到 7.2pt，横线、竖线全部完整；
每页多一次 150 dpi 渲染（A4 实测 **2.8ms/页**）。
回归：`test_translated_pdf_covers_ocr_block_with_white`、
`test_ocr_cover_never_clips_a_printed_rule_between_two_lines`、
`test_ocr_cover_band_bounds_the_translation`。

## v0.5.32

**「开始翻译 / 再次开始翻译」＝全新翻译，去掉多余的提示与确认。**
① **提示词与工具描述改为「直接开工」**：用户说「开始翻译/再次开始翻译/重新翻译/再翻一遍」→
立刻调 `run_translate`（重新提取 + 重新翻译 + 导出，按当前界面设置）；**不要先 `get_settings`
汇报设置、不要问「确认开始吗/要重新翻译还是重新导出」、不要复述「即将开始」**。
`get_settings` 的描述也改为「用户问当前设置时用，不要为了开始翻译先调它」。
② **按钮兜底**：若这一轮对话结束时 AI 并没真的启动翻译（只反问/只复述），
主窗口**自己直接启动全新翻译**并在日志写明「AI 未启动翻译，已直接开始全新翻译」——
用户按了按钮就一定会跑；AI 已启动时不会重复启动，用户随后发别的消息也会取消这个待办。
③ 回归：`test_start_translate_needs_no_confirmation`、
`StartButtonAlwaysStartsTest`（3 个：AI 没启动→自己启动 / AI 已启动→不重复 / 无关消息→取消待办）。

## v0.5.31

排查「**取消勾选「OCR表格重建为矢量表格」后重新翻译，输出仍是重建后的表格**」。
代码复核结论：选项在两条入口都已生效——「开始翻译」(`_start`) 与「重新导出」(`_re_export`) 都在启动时
**实时读取**复选框，且生产环境**不落盘任何缓存**（`PDFTRANSLATE_CACHE_DIR` /
`PDFTRANSLATE_OCR_CACHE_DIR` 未设置），所以重跑一定是**从头提取 + 重新 OCR + 重新翻译**；
也没有任何持久化的 `PDFTRANSLATE_*` 环境变量能强制打开它。真正的问题是**看不出来、也容易被路由错**：
① 每次运行（含「重新导出」）开头新增一行日志
**`导出选项：OCR表格重建=开/关，表格列宽重排=开/关`**——直接给出**本次生效值**，
用户据此一眼确认设置是否被读到（此前只能从输出文件反推）；
② 聊天提示词与 `re_export` 工具描述明确区分：「重新翻译 / 再翻一遍 / 从头翻译 / 重跑一遍 / 换设置再翻」
**必须**调 `run_translate`（重新提取 + 重译 + 导出，启动时读取当前界面设置，包括「OCR表格重建」）；
`re_export` 只是拿**上一次的译文**重新生成文件（秒级、不重译），只有用户明确说「按我刚才的修改重新导出」时才用。
用错工具会让用户以为「选项没起作用」。
③ 回归：`test_options_are_logged_with_their_effective_values`、
`test_hint_separates_retranslate_from_reexport`。
注：v0.5.25 之前构建的程序里「重新导出」**不传** `rebuild_table`，勾选/取消都无效果——若日志里
`导出选项` 与界面勾选不一致，请先确认运行的是 v0.5.25 及以上版本。

## v0.5.30

按审查报告 §4 第 6 条修完剩余 P1（5 条）：
① **会计负数金额不修复**（P1-5）：`(1,234.56)` 这种会计负数形式被 `_NUM_SHAPE_RE` 直接拒绝，
于是 `(3,702.726,474.45)` 这样的乱码**原样输出**、也**不被识别为数字格**（左对齐、还可能被送进
模型）。现 `_normalize_number` 先剥离一层括号/符号再判定，修复后原样拼回；`_is_numeric_cell`
也补「测试修复后的形式」分支。实测 `(3,702.726,474.45)` → `(3,702,726,474.45)`、
`（1,234.56）` → `(1,234.56)`，且都判为数字格。
② **OCR 白底矩形盖住照片**（P1-3）：`ocr` 块无条件画不透明白底，落在照片/彩色 logo 上就把图
挖掉一块。现改为**按像素判定**（`_region_is_paper`）：白纸上的印刷文字（扫描件）照旧画白底，
照片区域则**保留原图像素**（只画译文、不画白底）并记日志。整页扫描（面积 >50%）不走采样，
真机 p24 复检 125 块 / 126 个白底、无「图片区域」日志——扫描报表行为不变。
③ **reflow 列号语义错**（P1-8，勾选「表格列宽重排」时）：映射里的第三项是**行内单元格序号**，
合并单元格因此拿到第 0 列的框（实测表头 wrap 宽度 296pt→96pt）；没有映射块的列
`demand=0` 被算成**零宽**（两条竖线重合、列消失）。现从单元格 x 范围反推真实列号（跨列单元格
跳过重排），且 `demand=0` 的列保留原宽。
④ **表格内未映射块下推错位**（P1-9）：中心不落在任何单元格、但位于表格范围内的块被当成散文，
高度不计入行（多行译文压到下一行）、却按整张表的增量下推。现 `_locate_block` 在「精确单元格」
之后补一次「按行带归属」，其高度计入该行。
⑤ **双语译文页无图片/矢量**（P1-10）：译文页原本是空白页 + 文字，图文混排文档的译文页只有悬空
文字。现凡页面带**部分页图片**（照片/插图）或矢量绘图，译文页改为**源页副本 + 红action 文本**
（与原位导出同款），图片与矢量图保留；纯文字页仍用干净空白页。真机复检：年报第 1 页译文页
`images=3`、第 6 页 `images=1`（此前均为 0）。

## v0.5.29

按审查报告 §4 第 5 条修「**静默丢内容三连**」：
① **稀疏文本层合并会丢块**（P1-4）：`_merge_ocr_blocks` 原来按**整页文本集合**去重、
且「中心落在文本块内或 ≥20% 重叠」就丢——扫描报表里重复的数值（两行都是 `100.00`、
页码与某格数值相同）会被吞掉，而**落在文本块内的 OCR 块**（图片里烧进的字，如 logo 文字）
会被整行丢弃。现改为**文本 + 位置**双重判据：仅当 OCR 块文字与某文本块相同、
且该 OCR 块自身面积被它覆盖 ≥50%（新增 `_covered_ratio`，非对称）才判重复。
② **纯符号表格单元格被删**（P1-6）：`—` 是报表的「无值」标记，却被当噪音丢弃，
而表格 bbox 整体红action 又把原符号擦掉 → 该格永久消失。现**保留表格单元格里的纯符号**
（文本层框线表与 OCR 网格两处），只丢弃**单元格之外**的散落符号（`。`、`•` 等）。
真机 p24 复检：多出 1 个 `(+)` 单元格（此前被吞）。
③ **页底合法文本被当成手写签字**（P1-7）：旧判据只有「高 + 页底 30% + 含字母 + 无数字」，
于是页脚的 `Independent Auditor's Report`、页底大标题都会被剔除（并谎报为签字）。
现追加两条：高度须 ≥**3×** 行高中位数、且文本**短**（去空格后 ≤6 字符——手写姓名/花体，
而说明文字与标题不满足）；日志改为「疑似手写签字 N 处，已跳过（…若非签字请人工确认）」。
真机 p24/p27 复检：手写签字（2 字符、5.1×/6.4× 行高）仍被正确跳过。

## v0.5.28

**调小「OCR表格重建」里 AI 重建矢量表格的文本行间距**——多行单元格原本走
**段落**拟合（`in_table=False`）：行距用 1.35× 的松散段落值、字号下限是 7pt 的正文下限，
于是一个两行的长标签既压不下去也排不开，第二行会**越过下面的表格线**（用户截图即此现象）。
现改为**按表格单元格拟合**（`in_table=True` + 显式 `Block.line_leading = 1.0`）：
行距 1.0×、字号下限降到表格的 6pt、优先单行，且行高由 `cell_height` **用同一套 `_fit_block`
测量**（测量与绘制必然一致）。实测同一内容（3 行、窄列）：行距 9.45pt → 6.0pt、
行高 18.6pt → 13.9pt，第二行不再压线；几何重绘（`_draw_ocr_grid_page`）本就使用紧凑行距，
不受影响。
`Block` 新增 `line_leading` 字段（0 = 自动），`_line_leading`/`_measure_block_height`/
`_draw_translated_block` 全部支持显式覆盖；OCR 缓存块不带该字段时取默认值，**无需递增
`_OCR_CACHE_VERSION`**。

## v0.5.27

按处理路径审查报告 §4 的顺序继续修 P1，本轮是**结构判定**两条：
① **无框线表格识别不到**（`docs/处理路径审查-v0.5.25.md` P1-1）：`find_tables()` 默认只认框线，
中文年报常见的「项目 / 本期 / 上期」无框线表因此完全没有单元格几何——整列被并成一个块，
译文与数字行不再对齐，含字母的合并块还会把数字送进模型。现新增
`_page_tables`/`_borderless_tables`：框线策略无果时回退 `find_tables(strategy="text")`，
并用三道门挡误报（≥2 行 × ≥2 列且 ≥2 行填满多格；**内容行填满所有列的比例 ≥60%**；
单元格「文字宽/格宽」中位数 <0.6）。实测：合成无框线表被识别（单元格成块、行高扩展生效、
**不额外画表格线**——`borderless` 标记让导出跳过网格重画与表格区红action），两栏散文页与
章节标题页被正确拒绝，真机 28 页年报 **0 页误判**。
② **扫描页「表格 + 正文」被整体网格化**（P1-2）：`_reconstruct_ocr_grid` 的判据是整页的，
只要页面任意处有一列数字，表下的说明段落也会变成表格单元格（`in_table=True`、整行宽
`fit_width`），勾选「OCR表格重建」时还会给段落画表格线。现新增 `_split_grid_items`：
表格区域 = 数字行周围的**最大连续行段**，止于「单个宽且无数字的 OCR 项」（散文行）；
段外内容按普通 OCR 块输出（`in_table=False`，不参与网格、不画线）。`_reconstruct_ocr_tables`
与纯页判定/几何重绘同步只看 `in_table` 单元格——混合页因此**不会**被整页重绘（段落不会被丢）。
真机 p24-27 复检：网格块数/行数/纯页判定与修复前完全一致（这些页没有散文行）。

## v0.5.26

修复处理路径审查报告（`docs/处理路径审查-v0.5.25.md`）里的 3 个 P0——都会**静默**
破坏输出：
① **矢量图表被误判成表格 → 图表被删**：`find_tables` 只看框线，一个「网格线 + 单元格里有文字」
的图表会被当成表格，导出时第二遍红action 随后删掉图表本体（实测柱状图整根消失）。
现新增 `_table_has_chart_art`：表格 bbox 内出现「条状填充」（有色/深色、两维都 ≥2pt、宽度
< 60% 表宽、高度 ≥15% 表高）或**非轴对齐笔画**（曲线/斜线）即判为图表，提取期与导出期都
放弃表格处理（图表与标签保持原样），并记一条日志。真机 28 页年报复检：**0 页**误伤表格。
② **多行单元格的译文互相覆盖**：一个单元格的多行源文本原本拆成多个块，导出时都被锚定到
**行顶**，同一位置重复绘制（实测同一文本画了两遍）。现按单元格合并为一个块（`\n` 连接、
`single_line=False`），`_fit_block` 走 `_fit_exact_n`、只产生一个绘制框；同一基线上的多个
span 仍用空格连接、保持单行。真机第 7 页 2 个多行单元格复检：**0 处重叠绘制**。
③ **表格行高扩展越出页底**：`_compute_table_layout` 从不检查页底，靠近页底的表格（及其下方
正文）被推到页面之外，文字与表格线落在 mediabox 外、任何阅读器都看不到，而源文本已被擦除。
现按页面高度等比压缩本页行高（保底不增长），并记「表格译文超出页底，已压缩行高至 N%」。
真机第 7 页长译文实测：**0 行落在页底之外**。
另同步审查报告的 P0 状态与 `CLAUDE.md`。

## v0.5.25

修复「**OCR表格重建选项失效**」——勾选后输出的仍是扫描（OCR）表格。
原因是 v0.5.24 的 P1-6「稀疏文本层并入 OCR」：真实扫描报表页（如年报 p24-27）的文本层
只有页码「22」，合并后该页多出一个非 OCR 块，`_is_pure_ocr_table_page` 因此判整页「不纯」、
`redraw_ocr` 分支被静默跳过（页面上看不出任何提示）。现改为：**页面家具**（非 OCR、非图表、
≤12 字、不含字母/汉字、且位于 OCR 表格 bbox 之外）不再阻止重绘，由新增
`_draw_page_furniture` 在重绘页上按原 bbox 补画（页码不丢）；表内无字母短块仍走原位路径。
同时「重新导出」补齐 `reflow`/`rebuild_table` 两个旋钮（旧实现只传 `ocr`/`agent_mode`，
勾选后点「重新导出」同样输出扫描表格）。真机 p24-27 实测：勾选后输出页
`images=0`（扫描底图消失、网格线与译文为矢量），页码 22–25 保留在原位。

## v0.5.24

按代码审查报告修复 6 个 P0 缺陷（报告见
`docs/代码审查-v0.5.23.md`）：
① `run_translate` 把 requirement 里**提到**的页号当成整篇页范围（“翻译整篇，第5页图表保留原文”
只翻了第 5 页）——新增 `parse_explicit_scope`，只有「只/仅/仅限/限定/范围」或显式区间才收窄；
② 旋转页（`/Rotate 90/270`）的双语译文页用了旋转后的 `page.rect` 建页，y 超过页宽的块译文整段丢失
——改用未旋转 mediabox + 沿用 `/Rotate`；
③ `_check_layout` 的扁平索引在单页调用时恒为 0（拿第 1 页译文度量第 N 页的框）——与 `_audit_blocks` 同源；
④ OCR 网格 `fit_height` 在密集行上被 `band > y1-y0` 门槛丢弃（真机 824 格中 542 格），多行译文越过
表格线；`_fit_block` 现改为「行带装不下换行时画单行」，`_OCR_CACHE_VERSION` 6→7，两个验证脚本
按「推导行带」判定（旧门槛让「零越界」成为假绿）；
⑤ `check_translation.py` 的数字比对按值比较（全角数字/负号、亿/万↔million/thousand 不再误报致命），
分隔符错乱仍报；
⑥ 对话历史裁剪不再产出以 `tool` 消息开头的窗口（单轮工具调用超过 cap 时会被 API 拒绝 400）；
⑦ **导出不再生成重名副本**：目标文件已存在时**直接覆盖**（此前会写成 `名称(1).pdf`），
「重新导出」与首次导出行为一致；唯一例外是输出路径等于源 PDF 时仍另存 `(1)` 并记日志，
避免毁掉输入文件（v0.5.17/v0.5.21 的 `unique_path` 重命名逻辑随之取消）。

**同批修复的 P1（两批 9 条）**：①`save_translated_pdf` 不再因 `per_page` 偏短而静默丢尾部页；
②带币种/单位的金额（`US$1,234,567.89`、`1,234.56万元`）不再被断词切成两半（`_is_amount_atom`
在断词前判定）；③`_inject_terminology`/`_translate_special_page`/`_ai_self_check` 不再把「取消」
吞成「翻译失败」（`except ControlSignal: raise`）；④`Pillow` 进 `requirements.txt`，缺失时告警一次
而不是静默不压缩图片；⑤`run_flow` 的 `auto_fix` 改为 **opt-in**（「自检…」默认只读，须明说
「自动改/修一下」才写回覆盖层）；⑥稀疏文本层页（封面/扉页）的 OCR 结果**并入**文本层而非整页替换；
⑦`retranslate_blocks` 与 `translate_blocks` 共用页归属过滤，不再跨页改写；⑧预览「译文」侧改用
`peek_doc()`，不再在 GUI 线程对整本扫描件 OCR；⑨`_reconstruct_ocr_tables` 补「至少一整列数字」
守卫，扫描散文页不再被误判为表格而重绘；⑩**IR 文档级术语对中文年报真正生效**——原实现只收
「2–4 字的极大中文游程」，而真实中文句子是一整条长游程，候选因此恒为空；现改为 2–12 字滑窗 +
频次阈值 + **最长频繁 n-gram** 判定 + 虚词/数字编号过滤，抽到 0 条时记日志（不再静默）；
⑪三处**文案修正**：`self_check_page` 不再声称「只复核真正翻译过的页」；错误文案/工具描述里的
「数字/代码块」改为「数字格」（代码块检测从未实现）；`eval_harness` 明确必须显式选
`--pdf-only`/`--outdoc`，只给两个 PDF 的缺省模式尚未实现。

## v0.5.23

修复**翻译成功后异常退出**（`RuntimeError: wrapped C/C++ object of
type TranslateWorker has been deleted`，进程直接 abort）。`stopped -> worker.deleteLater`
在 worker 线程内释放 C++ 对象，而 `finished` 是排队投给 GUI 线程的，删除可能先到；此时
`_on_finished` 读 `_report` 就会落到 sip 上抛异常。`_report` 此前只在 agent 路径赋值，
因此 **IR 模式 / 非视觉模型回退 / 「重新导出」** 三种运行必然命中（v0.5.21 用户实测即 IR 模式）。
现改为：`TranslateWorker.__init__` 初始化 `_report = ""`；GUI 侧读字段统一走 `worker_field()`
（对象已删也能读到真值），对 worker 的 Qt 级调用统一先经 `MainWindow._live_worker()` 过滤。

## v0.5.22

修复「OCR 表格重建」在**混合页**上丢失内容——`redraw_ocr` 会把扫描
表格页重绘为干净矢量表（空白页 + 网格线 + 译文），但重绘只保留 OCR 表格单元；若该页还混有
`is_chart`（图表节点）或非 OCR（文本层脚注/页码）块，这些块会被**整页静默丢弃**。现新增纯页
判定 `_is_pure_ocr_table_page`：仅当整页每个块都是可重绘的 OCR 表格单元时才整页重绘，否则落到
原位导出路径，妥善保留所有块。修复上版曾因遗漏 `_extract_tables` 定义而弹出的「翻译失败」弹窗。

## v0.5.21

修复「重新导出」写到错误文件。首次导出走 `unique_path`，当目标名已存在时
实际输出落到 `名称(1).pdf`；「重新导出」此前从界面重新推导**基础名**并覆盖之，可能误覆盖一个
无关旧文件，而真正的最新 `(1)` 输出却留在原地不更新。现改为写回上一次**真正产出的文件**
（`_last_output`）：当界面目标与上次输出同族（同目录、去掉 `(n)` 后同 base、同扩展名）时，
覆盖那个实际文件；当用户改了目录/文件名/扩展名/类型时则尊重新设置。新增回归测试
（`resolve_reexport_target`）。

## v0.5.20

恢复**手写体签字不翻译**——扫描报表底部那类「高出一大截、纯字母无数字」
的签字笔画在 OCR 合成阶段即剔除：不翻译、不覆盖，扫描原样保留（此前会被拼音化并被白色矩形
盖住手迹）。底部带判据以**未旋转 CropBox** 为准——旋转页此前会拿页宽当页高、裁剪页会忽略裁剪，
都可能把带判到错误一侧。OCR 缓存版本 5→6，旧缓存自动失效并重新识别。

## v0.5.19

**OCR 表格重建改用 0.2.7 的做法**——扫描表格页重绘为干净矢量表格
（空白页 + 网格线 + 译文，行高按译文扩展；视觉模型在线时先由模型重建「译文 2D 网格」
并可选标注合并单元格，失败回退几何重绘），原先基于 `table_vision` 的矢量重建链整体移除；
同时合入一轮代码审查修复（OCR 去斜方向、段落末行误拆、含表格页矢量图形丢失、旋转页坐标、
审计门非法检查名、IR 组批/术语、评估链恒等假绿等，详见 `CLAUDE.md`）。

## v0.5.0

引入**工具分层**（原子工具 / 流程工具 / 组合流程）与 **AI 自由组合（Path B）**——
控制台 AI 可把一句要求分解成若干任务（混合调用单工具、翻译流程、自检/导出）依次执行；
**对话侧视觉打通**（新增 `render_page` 让模型直接看页面译文图，工具结果图会以 `image_url` 回注）；
特殊页默认自动翻译、不再逐页询问。

