# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 项目简介

Windows 桌面 PDF AI 翻译工具（Python 3.10+，PyQt6）。从 PDF 提取文本，通过 `models.json` 中配置的任意 OpenAI 兼容 `/chat/completions` 端点翻译，导出为双语 PDF、原位翻译 PDF、Markdown 或纯文本。界面、日志与注释大量使用简体中文——新增用户可见文本时保持一致。

## 常用命令

```powershell
pip install -r requirements.txt          # PyQt6、PyMuPDF、openai
python main.py ["C:\path\to\doc.pdf"]    # 运行程序（run.bat 为 Windows 启动器）
python -m unittest discover -s tests -v  # 运行全部测试
python -m unittest tests.test_pdfio.PdfioTest.test_bilingual_pdf -v  # 运行单个测试
python check_translation.py 原文.pdf 译文.pdf [--lang English] [--strict] [--skip 24-27]
                                         # 导出后体检：数字一致性（exit 1，**按值比较**：全角/单位倍率/
                                         # 千分位风格差异不算错，分隔符错乱仍报）/残留中文/章节编号/页数
                                         # （v0.5.38：扫描页只跳过数字比对，残留中文照查；双语产物按「源页 i ↔ 译文页 2i+1」配对，不再拿原文页当译文页比对）
python check_layout.py 原文.pdf 译文.pdf [--page 3,5-7] [--dpi 150]
                                         # 导出后**排版**体检（exit 1 = 重叠/出页/压线/漏画，exit 2 =
                                         # 用法错/页码越界）：字形带重叠、越出页面、压在源页印刷线上、
                                         # 字号过小、漏画、页数/页尺寸——判据全部与文档无关；
                                         # 旋转页按未旋转帧判界、采样前映射到渲染帧，双语产物按
                                         # 「源页 i ↔ 译文页 2i+1」配对（见 README v0.5.38）
python verify_real_run.py                 # 真机对照：用 models.json 的 qwen3.8 翻译 p24-27，
                                         # 测量表内格拟合结果（字号桶/行带越界）——需本地模型在线
python verify_selfcheck.py               # 真模型走查：对样例页跑确定性审计工具 + AI 复核循环（同样需模型在线）
python make_diverse_test_pdf.py [out.pdf] # 生成 5 页多样化测试 PDF（文本双栏 / 文本+图片+表格 /
                                         # OCR 文本页 / OCR 表格页 / OCR 混合页）——手动验证各处理路径用
```

测试离线运行：`tests/_helpers.py` 提供本地 mock chat-completions HTTP 服务（按块回显 `[n] MOCK:<原文>`），`build_sample_pdf()` 用于生成小型测试 PDF（拉丁 + CJK 文本）。测试模块：`test_settings`（配置解析）、`test_pdfio`（提取与导出/数字原子性/竖排标签）、`test_translator`（分批/对齐/缓存/致命错误/术语表）、`test_ocr`（OCR 管道与缓存/数字归一化）、`test_worker`（worker 信号契约与姓名列方向门控，直接同步调用 `run()`，无需 `QApplication`）、`test_ui`（`main_window` 中与 Qt 无关的纯函数）、`test_preview`（预览窗口纯函数：裁剪/坐标映射）、`test_agent`（agent 基础：状态骨架+工具注册表）、`test_session`（M1/M2：文档信息分型+DocumentSession 会话控制器）、`test_chat`（常驻对话）、`test_chat_tools`（文档上下文+交互聊天工具）、`test_check_translation`（校对脚本）。所有测试通过 `PDFTRANSLATE_CACHE_DIR` / `PDFTRANSLATE_OCR_CACHE_DIR` 把缓存重定向到临时目录——**新增涉及缓存的测试务必照做**，否则会污染开发者 home 并因热缓存而假绿。未配置 linter 或格式化工具。

设计文档在 `docs/`：`0.3.0-设计.md`（已定稿：AI 主导编排 + 确定性工具层 + 人类在环）与 `0.3.1-交互式翻译流程设计.md`（M1–M6 里程碑方案）——v0.3.x 的功能决策与「为什么这么设计」常驻参考，改动前先读对应版本。

## 架构

单向流水线，全程以扁平块列表对齐：

```
main.py → MainWindow (PyQt6)
  → TranslateWorker（QObject，moveToThread 到 QThread）— 保持界面响应
    → pdfio.extract_document_text → 扁平 blocks + 平行的 block_pages 列表
    → translator.TranslationEngine.translate_blocks
    → pdfio.group_by_page（扁平列表 → 每页列表）
    → pdfio.save_* 导出（按 worker.py 中 OUTPUT_TYPES 的键分发）
```

**控制主体定型（v0.4.0）**：AI 是**唯一控制主体**——对话是**控制台**（`chat.py` 的 `ChatSession`），翻译/质检/导出是**控制台授权后启动的「流程」**（`agent/flow.py` 的 `DocumentSession`：有界逐页循环 `max_rounds` + 确定性审计门 `audit_page`）；入口唯一（按钮→`_start_via_chat`→`run_translate`→`_start`），无绕过控制台的直达流水线路径。详见 `docs/0.4.0-控制台与流程.md`。

### translate_app/settings.py
- `models.json`（项目根目录）声明模型；`ModelConfig.from_dict` 解析。`api_key` 字段存**原始值**且 `repr=False`（repr/断言不会打印密钥）；形如 `${ENV_VAR}` 的占位符在 `_resolved_api_key` / `validate` 里**惰性**替换（不在 `from_dict` 时替换——加载时替换 + 使用时再替换的双重替换会造成不一致，如 env 变量被设为空串时会静默变 `"not-needed"` 且 `validate` 不告警）；未解析的占位符绝不会作为 API key 发送（改用 `"not-needed"`，且 `validate` 会告警）。
- `endpoint` 是**完整**的 chat-completions URL；`client_kwargs()` 去掉 `/chat/completions` 后缀得到 `base_url`，并默认 `timeout=300`。models.json 条目中未识别的键进入 `ModelConfig.extra`，透传给 OpenAI client kwargs（可覆盖 timeout）。
- 按模型可配：`temperature`（默认 0.2）、`max_tokens`、`concurrency`（默认 1，串行；云 API 提速可设 2–4）、`batch_size`（每批原文字符预算，默认 4000；本地慢模型加大可显著减少请求次数）、`page_concurrency`（agent 路径的**并行页翻译**并发数，默认 1＝串行；云端有富余时设 2–4 可近线性加速整篇，本地单卡服务端仍会串行排队）。
- `reasoning_effort`（以及未来的按请求参数）通过 `extra_body` 发送，使 llama.cpp 类服务端能收到；缺少 `reasoning_effort` 时 llama.cpp 会 500（见 README）。
- **按调用目的分两套请求参数（见 `docs/0.4.0-控制台与流程.md`）**：**控制台/对话侧＝仅侧栏文字聊天**（`chat.py` 的 `ChatSession`/`ChatWorker`），用 `interaction_temperature`（默认 0.6）与 `interaction_reasoning_effort`（默认 `"medium"`，经 `interaction_request_params()` → `extra_body` 发送）；**流程/生产侧＝其余一切**（翻译引擎 `translate_blocks`、agent 的 `decide` 调用 `make_llm_decide`、特殊页协商、预览、标注、导出），统一用 `models.json` 的 `temperature`（默认 0.2）与 `reasoning_effort`（`request_params()`）——`make_llm_decide` 用 `model.temperature`/`model.reasoning_effort`，**不用** `interaction_*`。可用 `interaction_temperature` / `interaction_reasoning_effort` 按模型覆盖（进入 `ModelConfig`，不落入 `extra`）。
- 用户偏好保存在 `~/.pdftranslate/prefs.json`（模型 id、语言、输出格式、上次目录）；`save_prefs` 原子写（临时文件 + `os.replace`，程序硬退不会留下半截 JSON）并返回失败原因（`None` = 成功），UI 侧保存失败会写入日志而非静默丢失（静默丢失的偏好要等下次启动才暴露、无法诊断）。

### translate_app/prompts.py
- **本应用是「由提示词构成」的 AI 中心应用**：所有 AI 决策——批次翻译、逐页 agent 编排、特殊页协商、常驻对话、工具使用——都由系统/用户提示词驱动。**所有提示词集中在此模块**，translator/agent/chat/main_window 一律 `import prompts` 调用，**不再在控制路径里内联拼提示词**，便于审阅、调优、版本化与复用。
- 目录：`translation_system_prompt(language, glossary)`（`translator._system_prompt` 的正文，含 `_EXAMPLE_INPUT`/`_EXAMPLE_OUTPUTS`——输出必须落在目标语言，非 ASCII 目标自行声明输出样例）；`agent_interaction_rules()`（agent 系统提示词追加的「该问就问」规则，含 `【与用户交互】`、`ask_user` 触发点）；`agent_tool_policy()`（**工具功能与用法参考**：在系统提示词里逐工具列出「功能 + 调用方式」——观察/修改/校验/交互四类归组、扁平 index 语义、set_text 写已知译文、retranslate_block **只返回译文需再 set_text**、delete_block 撤回为原文、改后必须校验、ok=false 先读 error 等）；`agent_workflow()`（**翻译通用工作法**：整篇逐页推进的『观察→构建/编辑→校验→复检→完成』方法——首遍翻译 get_doc_info/classify_page→read_page/render_page→apply_terminology→translate_block；复核修错按 check_* 问题清单用 set_text / translate_block / retranslate_block+set_text / apply_annotation / delete_block；每改一处复跑 check_* 直到无问题才结束）；`page_task(page_index, lang, kind=None)`（逐页任务，`kind` 为特殊页时附 `（kind 页）`；`page_task`/`review_page_task` 现在**强调整页所有文本块都必须译成 `lang`、整页最终应全部为目标语言**，且**必须**让 `check_residual`/`check_missing` 返回无问题后才能结束，避免“翻译不完全/提前结束”）；`special_page_question(page_index, kind)`（M3 特殊页的 `(问题, 选项)`，选项映射到 translate/keep/skip）；`content_policy_task(entries, lang, requirement)`（**内容策略**：把「该不该保留原文」有歧义的块列给模型，要求回 `release`/`keep` 索引——见「AI 内容策略」一节）；`chat_system_prompt()`/`chat_tool_hint()`（常驻对话系统提示词 + 有工具时追加的 `【可用工具】`；后者由 `ChatSession._call` 在 `tools` 非空时拼接）。**工具描述也是提示文本，统一收敛到 `agent/tool_catalog.py` 的 `TOOL_CATALOG`**（单一 `ToolDef` 目录：每项含 schema + description + audience）。agent 侧经 `agent.tools.agent_openai_tools`、chat 侧经 `chat_tools` 的 `CHAT_TOOL_SPECS`（`to_openai_schema`）各自按 `audience`（`"agent"` / `"chat"`）过滤出本侧可见的工具；共享工具（get_doc_info/classify_page/get_structure/get_table/apply_annotation）的 schema+description 只定义一次、两边共用；read_page 因 schema/返回场不同保留 agent 与 chat 两条。**注意**：工具「参数级」的 `description`（如 `bbox` 的“PDF 点”）仍随各 schema 内联，属参数定义而非工具级提示。
- **减少硬编码（非 AI 工具调用）**：面向用户的决策应尽量经 agent + 工具给出（`get_doc_info`/`classify_page`/`read_page`/`ask_user`/`set_block_text`…），而**纯确定性渲染/提取**（列聚类、表格排版、缩放、OCR 框还原）仍属工具引擎，不宜也不应交给模型。改动提示词就改这里，别散落在各处。

### translate_app/translator.py
- 翻译协议：每个批次以编号块发送 `[[1]] 文本 … [[k]] 文本`；模型必须回显 `[[n]] 译文`（一个块可跨多行，`_MULTI_BLOCK_RE` 折叠内部换行）。编号**仅在批次内局部使用**（1..k，而非全局索引），因为已缓存的块会被跳过。`_parse_response` **要么返回恰好 k 条译文，要么抛 `ValueError`**：有 `[[n]]` 标记时编号必须完整无缺、不越界；完全没有 `[[n]]` 标记时只接受「行数 == 块数」的回复（单块批次例外：整段回复折叠为该块译文）。**空译文同样被拒**（编号块无内容、或整段空白回复折叠成 `""`）——空串会被判成功、写进缓存并让该块在每次导出里变空白，且被永久复用。**绝不用原文填补缺口**——那会被判为成功并写入缓存，模型的一句拒答就能永久污染该块。**改协议前先看 `tests/_helpers.py` 的 mock**（按 `\n\n` 分块解析 user 消息）。
- 按字符预算分批（`_CHAR_BUDGET` 默认 4000，可按模型 `batch_size` 覆盖，`_make_chunks` 读取）；纯数字/符号块（`_needs_translation` 判空）不发送、原样保留。
- 系统提示词（`_system_prompt(language, glossary)`）面向中文年报翻译强化规则：数字/单位不得改格式（万元按「ten thousand yuan」保留数值）、章节编号沿用原文风格、报表/文件编号不得拼音化；目标语言为**西文**时额外要求中文人名统一拼音罗马化、**名在前姓在后**（如 王晓东 → `Xiaodong Wang`）。`glossary.json`（**文档所在目录**的扁平 JSON 对象 `源词→目标词`）随批次注入提示词；加载失败经 `log` 告警并忽略（静默跳过会让用户误以为术语已生效）。`translate_blocks` 的 `keep_original`（姓名列）由 **worker 按目标语言方向门控**：目标含 CJK 时保留原文，否则释放给模型按规则罗马化（并记日志说明）。
- 批次在 `ThreadPoolExecutor` 中并发请求（`model.concurrency` 个 worker）；结果按完成序回收，输出仍按输入块序对齐。缓存字典更新加 `_cache_lock`；**磁盘落盘在 `_cache_lock` 之外**进行（持锁取快照 + 单调序号，独立 `_persist_lock` 串行化 I/O，`seq < _persisted_seq` 的过期快照直接丢弃）——持锁做文件 I/O 会让并发批次互相阻塞。
- **引擎可复用（避免已关闭客户端）**：`translate_blocks` 在批处理的 `finally` 里**不再关闭** `self.client`——agent 层在**同一页内复用同一个 engine 多次 `translate_blocks`**，若每批结束后关闭 httpx 客户端，第 2 次联网调用就会用已关闭的客户端而报 `Connection error`（回归 `test_engine_is_reusable_across_batches`）。取消/中止仍由看门狗关闭客户端；进程以 `os._exit` 硬退，空闲连接池无害。
- **共享单个 OpenAI 客户端（减少连接池）**：`TranslationEngine.__init__(model, client=None)`、`make_retranslate_fn`、`make_retranslate_batch_fn`、`make_llm_decide`、`make_llm_interpret` 均接受可选 `client`——`run_page_visual` 每页创建**一个**共享客户端并注入 engine + 两个重译 fn + decide 循环，替代原先每个调用点各自 `OpenAI(...)`（一页 4 个连接池 → 1 个）。`client=None`（默认）时各调用点仍自建，测试 mock（`(model[, log])` 签名）不受影响。
- 瞬时故障（网络错误、408/429/5xx、解析不匹配）重试 3 次并带退避（`retry_delays`，测试注入 `(0,0)`）。重试耗尽后该批次**保留原文**（绝不丢内容）且**不写入缓存**（防污染 resume），失败原因记入 `result.errors`，由 worker 汇总成一条中文告警日志。
- **致命配置错误**（401/403/404：`_is_fatal`）不重试也不降级：设置 `abort` 事件让排队中的批次立即让路，并抛 `TranslationAborted`，worker 转成 `error` 信号。否则密钥填错会「成功」导出一份与原文逐字相同的文档。400（`BadRequestError`）视为单批次问题，仍只保留原文。
- **磁盘缓存仅内存、不进生产**：`translate_blocks` 的持久化**默认关闭**（`_cache_persist_enabled()`，仅当环境变量 `PDFTRANSLATE_CACHE_DIR` 被设置才读写磁盘——测试都设它来激活缓存），生产运行是**内存缓冲**：运行中只在内存累积译文、**不写任何中间缓存文件**，只保存最终输出；每次运行即全新、无跨运行复用（取消/崩溃即从头重译）。缓存目录 = `~/.pdftranslate/cache`（`PDFTRANSLATE_CACHE_DIR` 可覆盖；不可写时回退系统临时目录 `pdftranslate_cache`；`_cache_dir` 实写探针验证可写性）：文件名 = `trans_v{_CACHE_VERSION}_` + sha1(文档路径|目标语言|模型id|**术语表内容哈希**) 前 16 位，内容 = JSON 映射 md5(块文本)→译文。改动缓存语义需**递增 `_CACHE_VERSION`**（当前 9：5=术语表入键+名称西文罗马化，6=人名顺序改前，7=编号默认阿拉伯，8=CJK目标检测修复，9=批次提示词新增【上下文参考】邻接上下文段）令旧缓存失效——**术语表改变也换缓存名**。每批完成即落盘且**原子写**（临时文件+`os.replace`），非 dict 视为损坏忽略；进度从已缓存块数+跳过块数起始（仅缓存启用时）。测试用 `PDFTRANSLATE_CACHE_DIR` 指向临时目录并**以此激活缓存**。
- 缓存写入是尽力而为：失败**不中断翻译**，但会经 `log` 回调告警（每次运行只警告一次）。若不告警，只读 home / 沙箱等场景下会表现为「每次重跑都全量重译」而无任何线索。
- 取消抛出 `TranslationCancelled`（重试退避的 sleep 分片检查），worker 捕获后仅输出“已取消”日志（不算错误）；无论成功、失败还是取消，worker 都在 `finally` 里发 `stopped` 信号。`TranslationCancelled` 与 agent 的 `FlowCancelled` 都是 `translate_app.control.ControlSignal` 的子类（统一「控制信号」基类，见 flow_steps）。

### translate_app/pdfio.py
- `extract_document_text` 使用 `page.get_text("blocks")`，仅保留文本块（type 0）；`_order_lines` / `_order_blocks` 共用 `_order_generic` 按 x 重叠聚类分栏，多栏页按列（左列自上而下，再右列）阅读，单栏保持 y 取整后按 x。**整宽元素（横跨两栏的标题/分隔线）在聚类前先抽出**：贪心聚类里宽元素会成为右边界=页宽的「列」，两栏所有行都与之「重叠」而被并成一栏、左右行按 y 交错（`test_full_width_heading_does_not_merge_columns` 回归）；判据「宽度 > 1.5× 中位宽度」在单栏页永不成立、自动退化为纯 y 序，整宽元素按 y 插到列前（页眉）或列后（页脚）。每页 `page.get_text("dict")` 只提取**一次**，行收集器（`_collect_lines`）与 span 收集器（`_collect_spans`）共享同一 dict（曾各自重取、成本翻倍），为每个 `Block` 填充 `size`（span 字号中位数）、`bold`、`align`（left/center/right）与 `single_line`。`DocumentText` 同时携带 `pages`（带布局元数据的 `Block`）和扁平的 `blocks`/`block_pages`。
- CJK 文本用 PyMuPDF 内置的 `fitz.Font("cjk")` 渲染（回退 helv）。所有块（含粗体）统一用同一款字体——曾尝试用 `insert_text("china-s", render_mode=2)` 描边模拟粗体，但会混入第二种字体（Heiti），造成版面字体不一致，已移除；`Block.bold` 字段保留但暂不改变渲染。
- `_draw_translated_block`（原位与双语共用的绘制助手）：字形框（ascender+行+descender）锚定块 bbox 顶部，字号从原块字号起（上限 `_MAX_FONT` 40pt）以 0.9 因子缩减直到装进框内；**正文块的可读性下限是 `_MIN_READABLE`(7pt)**（`max(floor, round(fs*0.9, 2))` 钳制使下限恰好为 floor）；3pt（`_MIN_TABLE_FLOOR`）只出现在表格/竖排路径——**它不是「不溢出保证」**，病态长译文在 floor 仍超框时宁可溢出绘制也不丢内容；单行块在框内垂直居中，多行块贴顶（若多行块比框还高，居中偏移钳为 0，即贴顶）；支持左/中/右对齐。**表格单元（`in_table`）的拟合规则**：先按列宽（`fit_width` 或缺省框宽）单行拟合、缩至 `_MIN_TABLE_READABLE`(6pt)——单行 ≥6pt 时保持一行（数字/短译文永不换行）；**可读单行放不下时改为在多行下限 6pt 处换行，绝不再缩到 3pt 一条**（旧规则在扫描报表上产出 44 格 <5pt、5 格触底 3.0 的实锤软肋）；**网格单元格（`fit_height` > 0，见下）的换行以行带为界**（`_fit_band` 在 [[0.01pt 网格]] 上二分取得 ≤ `fit_height` 的最大字号），换行绝不越过下一条表格线；文本层单元格（`fit_height`=0）的换行由 `_measure_block_height` → 行高扩展吸收（矢量线可整体重排）；非表格块仍走 wrap 路径。**文本层表格的两个 v0.5.26 约束**：① `_build_table_blocks` **按单元格累积**——同一单元格的多行源文本合并为一个块（`\n` 连接、`single_line=False`、bbox 取并集），否则一个单元格出多个块、导出时都被锚定到**行顶**而互相覆盖（真机回归 `test_multiline_cell_lines_merge_into_one_block`）；同一基线上的多个 span 仍用空格连接并保持单行。② `_detect_table_cell_rects`/`_extract_tables` 都先过 `_table_has_chart_art`：`find_tables` 只看框线，会把「网格线 + 单元格里有文字」的图表判成表格，随后第二遍红action 删掉图表本体（实测柱体像素变白）——bbox 内出现「有色/深色、两维 ≥2pt、宽 <60% 表宽、高 ≥15% 表高」的填充（`_fill_looks_like_a_bar`；表格线是细填充矩形、底纹是白色填充，均被排除）或任何非轴对齐笔画即判为图表、放弃表格处理（回归 `ChartNotATableTest`，真机 28 页年报 0 误伤）。③ `_compute_table_layout` 的 `page_height`（传 **`page.cropbox.height`**，未旋转帧——**不是 `.y1`**：块坐标是 cropbox 相对坐标，`.y1` 含裁剪偏移，CropBox 原点非 0 时钳制永不触发，v0.5.39）把整页增量**钳制在页底之上**：不够时等比压缩每行额外高度并记日志，否则页底表格会把末行与表格线画到页面外（源文本已被红action，等于静默丢内容；回归 `TablePageBottomClampTest`）。**列号与行归属（v0.5.30）**：`_rebalance_table_columns` 的列号必须由单元格 x 范围反推（映射第三项是**行内序号**，合并单元格会拿到第 0 列的框；跨列单元格跳过重排），且 `demand=0` 的列并入「保宽」集合（否则宽度塌成 0、两条竖线重合）；`_map_blocks_to_table_cells` 走 `_locate_block`——精确单元格命中之后，再按「块中心 y 落在哪一行 y 带内」归属（要求块与表格 bbox 相交），否则跨越单元格的块被当散文：高度不计入行、却按整表增量下推（回归 `test_rebalance_keeps_a_merged_cell_and_an_empty_column`、`test_block_straddling_cells_is_mapped_to_its_row`）。**无框线表格（v0.5.27）**：`_page_tables` 在框线策略无果时回退 `find_tables(strategy="text")`，`_borderless_tables` 用三道门挡误报（≥2 行×≥2 列且 ≥2 行填满多格；内容行填满所有列的比例 ≥60%——章节标题页只有 3/7 行填满；单元格「文字宽/格宽」中位数 <0.6——两栏散文页的中位数为 0.72）；`_extract_tables` 给这类表打 `borderless: True`，`_compute_table_layout` 对其**跳过网格重画与表格区红action**（源本无线，重画会凭空加线）。回归 `BorderlessTableTest`，真机 28 页年报 0 误判。④**纯符号单元格保留（v0.5.29）**：`_build_table_blocks` 与 `_reconstruct_ocr_grid` 都保留单元格内的纯符号（`—` 是报表的「无值」标记，本就不会被翻译但**必须画出来**——表格 bbox 的红action 会把原符号一起擦掉），只丢弃**单元格之外**的散落符号（`。`/`•` 等噪音）；回归 `SymbolCellTest`，真机 p24 复检多出此前被吞的 `(+)` 格。**OCR 混合页（v0.5.27）**：`_split_grid_items` 把「数字行周围的最大连续行段」当表格区域，止于单个宽（≥0.6 页文本宽）且无数字的散文行；段外 OCR 项由 `_ocr_plain_blocks` 输出为 `in_table=False` 的普通块；`_reconstruct_ocr_tables`/`_is_pure_ocr_table_page`/`_draw_ocr_grid_page` 与重绘分支统一只认 `ocr and in_table`（散文块因此既不进网格、也不会被整页重绘丢弃；回归 `OcrMixedPageGridTest`）。
- `save_translated_pdf`（仅译文/原位）：原样插入每页原文，涂盖原文文本矩形但保留图片与线条（`PDF_REDACT_IMAGE_NONE` / `PDF_REDACT_LINE_ART_NONE`），再按块调用 `_draw_translated_block`。对 `Block.ocr` 为真的块（其文字是位图、无文本层可红action）**不红action**，而是先在该块 bbox 画一个不透明白色矩形盖住扫描字，再画译文，避免叠字——**白底只在「纸」上画（v0.5.30）**：`_photo_ocr_blocks` 对「部分页图片」（面积 ≤50% 页，`_partial_image_rects`）内的 OCR 块按像素判定（`_region_is_paper`：页面 72dpi 渲染后采样该块区域，纸白像素占比 ≥60% 才算纸），照片/彩色 logo 上的文字**保留原图像素、只画译文不画白底**（此前无条件白底会把照片挖掉一块）；整页扫描不采样、采样失败一律保留白底（安全方向）；**旋转页按渲染帧采样（v0.5.39）**：`_photo_ocr_blocks` 先把块框经 `_rot_map_rect` 映射到 `get_pixmap` 的渲染帧再判「纸 / 照片」——此前直接拿未旋转框采样，在旋转页上取到的是转置后的位置（实测读到白纸），于是**白底把照片挖了个洞**。**白底贴合实测字形带（v0.5.33）**：OCR 框上下带留白、在扫描报表上常顶到表格线，整框白底会把位图表格线一起擦掉（用户截图：白框切断虚线）。改为按 `_INK_SAMPLE_DPI`(150) 渲染该页一次、用 `_ink_bands` 采样实测字形带，白底按**每个文本行**绘制（机制细节见下条 v0.5.34）。**白底/译文避让表格线（v0.5.34，真机对照后细化）**：① 字形带 = **深色核心**（`_INK_LUMA`=140，滤掉扫描噪点）再向上下各**生长 ≤ `_INK_EDGE_PX`(9px≈4pt) 的印刷行**（`_INK_EDGE_LUMA`=200，绝不进入表格线行）——只测核心会把淡笔画留在译文底下（真机放大可见残影）。② **页面级表格线掩膜 `_page_rule_mask`**：印刷游程**又长又细**（`_RULE_MIN_RUN_PX`=30px≈14pt、厚度 ≤`_RULE_MAX_THICK_PX`=6px）且**自身有一段 ≥`_RULE_MIN_SOLID_PX`(6px) 的实线**（扫描纸面的点状纹理是 1–2px 点，闭合成游程后会被这条挡掉；否则整条文字行都被判成表格线、白底碎成细条），虚线先按 `_RULE_GAP_PX`=4 闭合；长度/厚度都在**同时做横竖闭合的掩膜**上量（只量横向闭合掩膜时，填缝像素自身没有纵向厚度 → 整行文字被误判成表格线，实测踩过）。③ 白底 = 「每个文本行 × 避开竖线的 x 段」的**矩形集合**（多行框得到多块），竖线列要求「在本块内占比 ≥0.3 **且延伸到本框之外**」（字形竖笔也细长，但它止于框内）。④ 译文高度被**下一条表格线**截断（`_rule_below` + `Block.fit_height`），否则 OCR 行带（按文字顶部推出、含那条线）会让换行第二行穿过虚线。⑤ **单行源块不换行压线**：`_fit_block(..., avoid_below=…)`——该块下方最近表格上框线若会被换行第二行越过，就保持一行、降到 `_MIN_READABLE`、横向溢出到旁边空白（右对齐行改为**向左**溢出，`x = r.x1 - lw` 不再夹到框左边）；调用方在 `save_translated_pdf` 里用「表格 bbox 顶边 + OCR 表格线」算出 `avoid_below`。真机复测：残影消失、表格线被擦像素 417→173（0.20%，余下多为印章/表号区）、`单位：人民币万元` 2 行→1 行且不压线。回归 `test_single_line_label_above_a_table_stays_one_line`、`test_faint_scan_text_is_covered_too`。**全部改为自适应（v0.5.35，通用性要求）**：阈值与判定**不得**依赖某一份文档的对比度/尺寸/朝向——① 亮度基准 = **局部纸面**（`_local_paper`：16pt 网格的 90 分位，再取 3×3 邻域**最小值**，白页边距不能抬高紧邻灰扫描区的基准）+ 页面稳健对比度 `span`(p90−p1)，派生三个偏移量（印刷 `max(8,0.22span)`、核心 `max(20,0.50span)`、抗锯齿边 `max(6,0.15span)`），清洁扫描/灰复印件/淡传真共用一套代码；② 表格线判据全部用**点**（`_RULE_MIN_RUN_PT`=10、`_RULE_MAX_THICK_PT`=3、`_RULE_GAP_PT`=2、`_RULE_MIN_SOLID_PT`=3）换算像素，与采样 DPI/文档尺寸无关；③ `_page_rule_mask` 对**每一页**都算（渲染一次 150dpi ≈3ms/页），因此**文本层页面**上「标签下方压着下划线」也被 `avoid_below` 避让（不再只依赖 `find_tables`）；④ **旋转页**经 `_rot_map_rect` 把块框映射到渲染帧采样、白底矩形再映射回来，旋转后的表格线按「细长竖条」识别（`_ink_bands(tight_x=True)`），此前旋转页一律回退整框白底、必然压线；⑤ 照片/纸判定 `_region_is_paper` 改为**局部**（区域自己的纸面须主导该区且 ≥ 页面纸面的 75%），灰纸扫描仍算纸、均匀深色照片不再误判；⑥ 右对齐长行横向溢出钳在页内。回归 +4：淡扫描紧贴白底、深色扫描纸面不算墨、旋转扫描不压线、下划线（非表格）上方标签不折行。**版面体检驱动的两条修复（v0.5.36）**：① `avoid_below` **只是高度预算、绝不拉长框**——曾把框的 y1 拉到下一条表格线，而单行译文在框内垂直居中，于是整行下沉（真机 +10pt，扫描件标题下沉 13.6pt 压住副标题）；`_fit_block` 现用它限制字号/换行（`box_h = min(r.height, avoid_below - y0 - 0.5)`），框保持原位。② OCR 网格 `fit_height` **下限 = 单元格自身字形高度**（v0.5.36；**v0.5.40 已撤销**——抬高行带等于放宽*换行预算*，第二行会压进下一行；现在 `fit_height` 恢复为`next_top - y0 - 1.5` 的真实行距，可读性改由 `_fit_block` 处理：行带装不下换行时**画单行**、字号可用字形高度（实测同一格 3.0 → 6.2pt））。**改 OCR 网格几何必须递增 `_OCR_CACHE_VERSION`（当前 9：v8 = 行带下限抬到字形高度，v9 = 行带恢复为真实行距、单行改用字形高度，见下）**——缓存里存的就是 fit_height/fit_width/bbox，不递增则已 OCR 的文档永远用旧几何。回归 `test_rule_bound_does_not_move_a_single_line_translation`、更新的 `test_tight_rows_keep_a_band_and_stay_inside_it`。真机体检（`test.pdf`→译文）：两页 0 重叠 / 0 出页 / 0 压线，第 1 页无 <7pt，第 2 页逐格垂直偏差中位数 0.32pt。**遍历 `range(src.page_count)`、`per_page` 短也不丢页**（`per_page[i] if i < len(per_page) else []`，无译文的页原样插入；旧实现 `min(src.page_count, len(per_page))` 会静默截断文档，回归 `test_translated_pdf_keeps_pages_without_translations`）。
- `save_interleaved_pdf`（双语）：原文页之后是译文页，**译文块按原文块 bbox 绘制**；无文本页写提示文案。worker 传 `doc.pages` 供其使用。**译文页保留图片与矢量图（v0.5.30）**：页面带**部分页图片**（照片/插图，面积 ≤50% 页；`_partial_image_rects`）或矢量绘图时，译文页改为**源页副本 + 红action 文本 + 画译文**（与原位导出同款，OCR 块按 `_photo_ocr_blocks` 决定是否画白底），纯文字页仍用干净空白页；此前译文页恒为空白页 + 文字，图页的译文页只有悬空文字（回归 `test_bilingual_translation_page_keeps_a_figure`）。**译文页必须用未旋转 mediabox 尺寸并沿用源页 `/Rotate`**（`Block` 坐标在未旋转帧；用 `src[i].rect` 在 90°/270° 页上会交换宽高、把 y>页宽 的块画到页外，回归 `test_bilingual_pdf_rotated_page_keeps_every_block`）。
- OCR（扫描页）：`extract_document_text(...)` 接受 `ocr` / `ocr_fn` / `cancel` / `log`。当页无文本层且 `_needs_ocr(page)`（有图片或图形）为真时，用 RapidOCR（`rapidocr_onnxruntime`，内置中英文 PP-OCRv3 模型、离线）识别。**稀疏文本层页（≤3 行）的 OCR 结果是「并入」而不是「替换」**（`_merge_ocr_blocks`：保留文本层，只补**真正新**的 OCR 块并按 y,x 排序）——封面/扉页的标题曾因整页被 OCR 替换而在 Markdown/纯文本里整段消失；**判重必须「文本 + 位置」双条件（v0.5.29；文本条件 v0.5.40 起为「互相包含」——文本层把两行标题并成一块、OCR 逐行返回时完全相等判不出来，标题会被加两遍）**：仅当 OCR 块文字与某文本块相同（或互相包含）、且该 OCR 块自身面积被它覆盖 ≥50%（`_covered_ratio`，非对称；旧的整页文本集合 + `_boxes_overlap`「中心在内/≥20% 较小面积」会吞掉报表里重复的数值，并丢弃图片里烧进的字——回归 `test_repeated_value_survives_the_sparse_merge`、`test_ocr_block_inside_a_text_block_is_kept`）；纯扫描页（无文本层）仍是 OCR 结果即整页（`ocr_count` 只统计这类页，回归 `test_sparse_text_layer_is_merged_with_ocr_not_replaced`）。生产路径：`_page_to_array` 按 `_OCR_DPI` 渲染并做 **RGB→BGR**（RapidOCR/OpenCV 约定；`[::-1]` 翻转得到的是负步长视图，必须 `np.ascontiguousarray` 转连续数组再交给引擎），`engine(img)` 输出经防御式解析（兼容 `(list, timings)` 或裸 list），像素框 `/zoom` 转成 PDF 点，再经 `_synthesize_ocr_blocks`（`_clean_text` 清控制字符 → **逐条校验框坐标** → `_order_blocks` 按列阅读序 → 字号 `(y1-y0)/1.2` 截断 `[5, _MAX_FONT]`）还原成 `Block`（`ocr=True`、`single_line=True`）。**框坐标校验（v0.5.38）**：`ocr_fn` 是注入缝、VLM 后端返回的框不可信——NaN/inf 坐标曾一路进 `Block` 并在导出时抛 `ValueError: cannot convert float NaN to integer`（整轮失败，模型已翻完全篇），畸形/扁平框（`[x0,y0,x1,y1]`）曾抛 `TypeError` 被 `_ocr_page_blocks` 当成「OCR 失败」而**丢弃整页**其他块；现在逐条 `try` + `math.isfinite`，非法项跳过并记「N 个 OCR 框坐标非法，已跳过」（回归 `OcrBoxValidationTest`）。**OCR 结果按文档缓存**（`~/.pdftranslate/ocr_cache/ocr_v{_OCR_CACHE_VERSION}_<sha1>.json`，key 含文件 mtime+size；`PDFTRANSLATE_OCR_CACHE_DIR` 可覆盖目录）。**持久化默认关闭**（`_ocr_cache_persist_enabled()`，仅当 `PDFTRANSLATE_OCR_CACHE_DIR` 被设置才读写磁盘——测试设它即激活；生产是**内存缓冲**、不写 OCR 缓存文件，扫描页每次运行重新 OCR）。**改动缓存块字段语义时递增 `_OCR_CACHE_VERSION`**——OCR 缓存文件名无版本号时，旧版本写入的块（如无 `in_table` 的预网格重建格式）会被逐字复用，单行布局/列加宽逻辑对已缓存的文档全部失效（表现为扫描版资产负债表整表换行）；递增版本令其全部重 OCR。且**每识别完一页就原子落盘**（临时文件 + `os.replace`）——OCR 是全流程最慢的一环，取消或崩溃绝不能让已识别的页面白做；缓存对注入的 `ocr_fn` 同样生效，因此缓存逻辑本身可被测试。`DocumentText.ocr_count` 记录 OCR 页数供 worker 日志。`_get_ocr_engine` 惰性单例（`_OCR_LOCK` + `_OCR_FAILED` 缓存，失败返回 `None` 并**降级跳过**而非抛异常，由 `_warn_ocr_unavailable` 全局告警一次——该函数是唯一改写 `_OCR_WARNED` 的地方，模块级可变状态必须在带 `global` 声明的函数内改写）；`ocr_fn(page_index, page)->[(box,text)]`（box 为 PDF 点）是测试注入缝；`cancel()` 每页触发抛 `TranslationCancelled`。`_ocr_page_blocks` 里单页识别失败**降级为空并经 `log` 报出原因**（静默 `except` 与「这页本来就没字」无法区分），但 `TranslationCancelled` 属控制信号，必须原样上抛。**OCR 语言自动跟随原文**（RapidOCR 默认中英文自动识别模型，无独立选择项）。
- `detect_language` / `classify_page` / `get_doc_info`（M1，`pdfio.py`）：纯确定性的文档信息与逐页分型，供 AI 交互会话的 PREPROCESS 用。`classify_page` 按块标志判定 `normal/scan/chart/uncertain`（全 ocr→scan——扫描报表/表格仍属特殊页；≥3 个窄高竖排标签→chart；混 ocr+文本→uncertain；**其余含非扫描/文本层表格页均→normal**，正常表格不作特殊页、正常翻译即可——`table` 分型已从 `classify_page` 移除）；`get_doc_info` 返回页数/标题/语言/文本页/扫描页/图表页/待确认页/块数/每页类型（`table_pages` 恒为 0，保留字段兼容）。不调用模型。
- `DocumentSession`（M2+M3+M4，`agent/flow.py`）：文档级会话控制器，把 `WorkflowState` 按 `phase` 状态机推进：`preprocess → translate_normal → special_pages → completed`（**`review`/自检已从标准流程解耦**——“开始翻译”只做 翻译+特殊页协商+完成报告，**不再自动询问“是否自检 / 是否导出”，也不再逐特殊页询问用户**；`state.summary` 由 `_completed` 生成并在 worker 里打印给用户。自检/复核是另一个动作，用户一句话要求时经 `ai_self_check`/`self_check_page` 流程（或聊天 `self_check`/`run_flow`）**单独触发并报告结果**——见下方 M4 `_review`（现为按需调用，不再进标准阶段序列）。）**先翻译所有 `normal` 页（每页经注入的 `translate_page`，默认 `run_page_visual`）**（`model.page_concurrency`>1 时这些 normal 页经 `ThreadPoolExecutor` **并行**翻译：各页写 `out_doc` 的**不相交**扁平索引区间、`run()` 预置 `out_doc={}` 避免惰性初始化的竞态、每页用 `max_rounds` 硬上限替代共享 `state.budget` 计数器——否则并行下多页共享 budget 会互相重置/累加导致页过早停止；`make_page_executors` 的扁平块列表/索引映射每页只构建一次，不再逐块重展平）；随后 `special_pages` **逐特殊页默认自动翻译**——扫描/图表/不确定/公式/图片页一律经 `_translate_special_page`（`translate_page` 流程，每页带 `kind` 上下文）自动翻译，**不再逐页询问用户**（无预览弹窗、无 `ask_user`）；用户对某页/某类页的具体要求（如“第5页图表保留原文”）经 `state.requirements` 注入，由翻译 agent 读取后自行处理（必要时仍可 `ask_user`）。**M4 `_review`**：进入 `PHASE_REVIEW`，问「AI 自检 / 我手动检查」（`prompts.review_mode_question`）；`ai`→`_ai_self_check()` 逐页用 `run_page_visual` 回读（`prompts.review_page_task`：先 `read_page`+`render_page` 把该页取到眼前，再做**四类确定性审计**——①结构/版面 `check_layout`、②漏译 `check_residual`/`check_missing`、③数字校对 `check_numbers`、④表格/文字完整性 `check_table`——+ 就地 `set_text`/`retranslate_block`/`apply_annotation`；**跳过用户选择保留/跳过的页**——这类页是原文直拷、无 AI 译文可查，也避免把本意保留的原文误译为“残留”；且四个检查**统一跳过保真块**——`_protected`＝（引擎跳过翻译的 `_needs_translation` 为假）∪（改写门禁 `pdfio._is_numeric_cell`），否则复核循环会被数字格卡死【set_text/translate_block/retranslate_block 都拒改数字、delete_block 无对应块】——`review_page_task` 也注明这类块“不属于漏译”）。**检查语义（v0.3.3 起）**：`check_residual` 的目标语种按**身份**判定（`prompts._is_cjk_language`，不扫描语言名——默认中文目标名是 ASCII「Simplified Chinese」，旧谓词曾把中文译文整页报残留）——西文目标看残留中文、CJK 目标对称看未译的英文成句（`_is_latin_prose`：代码/缩写不算残留）；`check_numbers` 按**数值**比较（`_number_signature`：全角/千分位/单位倍率（亿↔million、万↔thousand）/月份与日期展开/Unicode 负号归一——“3.14 亿元”与“314 million yuan”按值相等，漏改/多改数字仍报）；`check_layout` 用**导出对齐**规则：表格 floor=`_MIN_TABLE_FLOOR`(3pt)、正文 floor=`min(块字号, _MIN_READABLE)`——合法的小字号脚注与行带内换行不再误报；**扁平索引偏移必须与 `_audit_blocks` 同源**（`offset = sum(len(pg) for pg in src.pages[:page_indices[0]])`——旧实现 `offset=0` 只在 `page=None` 的多页循环里累加，单页调用时索引退化为页内序号、拿第 1 页的译文去度量第 N 页的框：假绿 + 改不动的假红，回归 `test_check_layout_uses_flat_indices_on_a_single_page`）；每块只跑一次 `_fit_block`（高度从其结果推导，不再二次测量）；`crowding` 只计**同列**（x 重叠）的下一个块——两栏页不互相当作相邻；overflow/crowding 独立成检（原 elif 使 crowding 恒不可达）；竖排标签块跳过（导出器旋转绘制，不走水平拟合）），复核范围由调用方给出（保留/跳过页除外，不按「是否已成功翻译」过滤）；`user`→记 `review_mode="user"` 让用户在预览/侧栏处理（复用 M5/M6）。随后问「导出？/ 继续检查」（`prompts.review_export_question`），`继续检查`→`review_mode` 置 `user`（`worker` 仍在 `run()` 后导出——这是已知限制：会话无法阻止 worker 导出，真正「不导出」需 EXPORT 阶段门控）。逐页 `progress` 上报；取消即抛 `TranslationCancelled`。`worker._run_agent` 改为 `DocumentSession().run()`，`translate_page` 在调用时按 `agent_mod.run_page_visual` 解析，保证测试可 mock。
- `agent/tools.py`：新增 `get_doc_info` / `classify_page` 只读工具，并在 `make_source_tools` 绑定（读 `state.src_doc`）。`agent/state.py` 新增 `DocInfo` / `PageTriage` / `PHASE_*` 与 `WorkflowState` 的 `phase`/`doc_info`/`triage`/`current_page`/`summary` 字段。`preview.PreviewBridge` 新增非阻塞 `show_page(page, what)`（`get_region` 仍阻塞等待框选）。（清理：`Budget.max_passes/cost_cap/used_passes/cost`、`PHASE_EXPORT`、`WorkflowState.annotations` 为死字段，已移除；`PHASE_REVIEW` 与 `WorkflowState.review_mode` 在 M4 复核阶段重新启用。）`WorkflowState` 带 `_lock`（`threading.Lock`，`record_op` 用它把 `ops.append` + `budget.used_steps += 1` 变原子，避免并行页翻译丢计数）；`Budget.used_steps` 仍是**共享、逐页重置**的计数器——`page_concurrency>1` 下其值仅信息性（日志/`max_rounds=None` 回退），真正的每页上界是 `FlowAgent.run(max_rounds=…)`。
- **AI 工具有效性铁律**：暴露给模型（`agent_openai_tools(names=绑定名)`）的工具**必须**满足「a) 真被 `run_page_visual` 绑定、b) 后果确实进入导出/渲染」。注册表 `AGENT_TOOLS`（24 个＝`make_source_tools`+`make_page_executors` 绑定集，含 M4 复核新增的三个确定性审计工具 `check_numbers`/`check_table`/`check_layout`，以及 v0.3.4 新增的**聚合审计** `audit_page`——一次跑多类并返回带 `check` 标签的 findings）与绑定集**严格对齐**（测试 `AGENT_TOOLS` vs 绑定集），绝不暴露「声明了却用不了」或「调了却没效果」的工具。因此**已移除**这类工具：`set_font`/`set_align`（只写 `out_doc.size/align`，渲染器不读，对 PDF 零效果）、`rewrite_block`（忽略 `instruction`，等于重译）、`create_block`（≈`set_text`）、`move_block`（把文本错位到错误位置）、以及整批绘制/擦除/页操作/`render_region`/`qa_render`/`audit`（未绑定，模型看不到）。`classify_block`（连同 `make_agent_tools`/`make_classify_tool_fn`/`_CLASSIFY_TOOL`，只在独立分类路径且从未接入 `run_page_visual`）也在清理时**一并移除**——它是死代码。
- **确定性审计引擎 + 复核门（v0.3.4，`agent/flow.py`）**：四类复核检查（`check_residual`/`check_missing`/`check_numbers`/`check_table`/`check_layout`）是模块级函数（`_check_*`，单一来源，唯一入参 `(state, page)`），`make_page_executors` 里的同名工具只是对它们的**薄包装**，绝不重复实现。聚合器 `audit_page(state, page, checks=None)` 按 `checks`（默认全五类）跑一遍并合并成 `{"checks_requested", "checks", "issues", "clean"}`——`issues` 是带 `check` 标签的列单项，既供机器判断（`clean`）也供注入提示词。**`_ai_self_check` 由「逐页必跑 agent」改为「审计门」**：先用 `audit_page` 对每已译页做确定性审计，**干净页直接跳过**、只有**有 findings** 的页才交给 `run_page_visual` 修正；`prompts.review_page_task(page, *, findings, auto_fix)` 现**接收 `findings` 作为具体数据**（模型对着问题清单修，不再自己重新推导四类检查——修复「模型忘了跑某类检查就假绿」），`auto_fix=False` 时只审计不修改；改后**确定性复核**循环（`REVIEW_MAX_ATTEMPTS`=3 上限，达成 `clean` 即结束）。`audit_page` 也在 `AGENT_TOOLS` 注册、`make_page_executors` 绑定（agent 可自行调用）。**判 clean 必须 `audit.get("clean") is True`（v0.5.42）**——`get("clean", True)` 会把「审计没返回结果」读成「复核通过」。`DocumentSession.__init__` 新增 `audit` 注入点（默认 `audit_page(self.state, ...)`，测试可用假审计控制哪些页进入复核）。
- **流程骨架（Phase 2，`agent/flow_steps.py`，仅注册未迁移）**：`Flow`（name/description/steps/params/guards/scope）＋步骤类型 `ToolStep`（确定性工具调用，默认 label=工具名）/`AgentStep`（有界子代理循环，`task` 可为字符串或 `callable(FlowRunState, params)->str` 以注入 findings）/`UserStep`（阻塞询问用户）/`LoopStep`（until 或 max_iter）/`IfStep`（cond 分叉）。执行器 `run_flow(flow, *, tools, run_agent, ask, log, cancel, max_steps, params)` 与 `WorkflowState` **解耦**（调用方把绑定的工具表/agent 运行器/问答通道注入），返回 `FlowRunState`（applied/result/steps/error/ok）；`{{name}}` 占位符按 `params`（`flow.params` 合并每次 `params`）解析，**整串为单一占位符时按原类型返回**（`{"page":"{{page}}"}`→`3` 而非 `"3"`）。`FlowCancelled` 作为控制信号上抛（它是 `translate_app.control.ControlSignal` 的子类，与 `TranslationCancelled` 同基；`_Executor._tool`/`_agent`/`_user` 与 `run_flow` 都 `except ControlSignal: raise`，所以确定性 `ToolStep` 抛 `TranslationCancelled` 也会**作为控制信号上抛**、绝不落入 `except Exception` 被吞成 `error`），预算超限 fail-closed。注册表 `STANDARD_FLOWS`（数据：`preprocess`/`translate_page`/`translate_normal`/`special_page`/`special_pages`/`self_check_page`/`ai_self_check`/`export`）。**阶段 3（迁移，`agent/flow.py`）**：`DocumentSession` 的**单页单元**已改走 `run_flow`——`_translate_normal`/`_translate_special_page` 每个已译页/特殊页跑 `translate_page` 流程（`_page_agent` 绑定 `translate_page`+会话通道，把 `TranslationCancelled`→`FlowCancelled` 上抛、`except` 不吞控制信号）；`_ai_self_check` 每已译页跑 `self_check_page` 流程（`tools={"audit_page": lambda page, checks=None: self.audit(...)}`，`run_agent=self._page_agent`）。**注意**：`run_flow` fail-closed 会把异常吞成 `rs.error`，故 `_translate_normal`/`_translate_special_page` 需**判 `rs.ok`** 决定 `STATUS_DONE`/`STATUS_NEEDS_USER`；`_ai_self_check` 也**判 `rs.ok`**——审计工具自身失败（非"干净"）绝不能误判为"已复核"（否则复核没跑却放行）。外层阶段循环（`DocumentSession.run` 的 phase 状态机、特殊页协商、progress/record_op）仍为 Python 编排，`translate_normal`/`special_pages`/`ai_self_check` 这些**循环式流程**已注册为数据供后续/用户自定义流程复用。
- **用户自定义流程（Phase 4，`agent/user_flows.py`，U1 路径 A 参数化）**：`FlowSpec`（base + checks/scope/include_kept/auto_fix/page/lang/kind/output_type，`to_dict`/`from_dict`）；`compile_from_user(req, llm=None)` 用**规则解析**把一句中文填成 `FlowSpec`（`数字/表格/版面/漏译`→checks 子集——**无单字别名**，`数`/`表` 已移除以免误匹配「数据/代表」、`第3到第8页`/`第5页`→scope(0 基)、`把保留页也算`→include_kept、`只查/只读/不修改`→auto_fix=False（**页范围提示词 v0.5.42 起只认限制短语**：`只翻/只查/只看/仅翻译…`、`仅限/限定/范围`、紧贴「第N页」的只/仅；裸 只/仅 会误匹配 只要/不仅/不只，曾把整篇请求收窄成单页）、`重新导出`→base=export、`重译`→base=translate_page）；`llm`（可选，`make_llm_flow_compiler(model, client)` 返回）是**AI 槽填充**分支——模型把任意措辞解析成 `FlowSpec` JSON、规则解析为离线回退，失败闭到 `{}`（`_spec_from_ai` 校验降级）；`build_flow(spec)` 克隆标准流程并覆盖旋钮（单页流程配多页 scope 时用 `ForEachPage` 包成页循环）；`validate_flow_tools(flow, available)`＝**先绑定后暴露**门禁（`flow_tool_names` 递归收集 ToolStep，非绑定且非 `DETERMINISTIC_TOOLS={export,re_export}` 即为未绑）；`save_flow_spec(name, spec)`/`load_user_flow_specs()`/`get_user_flow(name)`＝**沉淀**为命名流程，二者磁盘读写经 `_flows_persist_enabled()` **环境变量门控**（`PDFTRANSLATE_FLOWS_DIR` 未设时只驻内存、绝不隐式写盘；测试设临时目录即激活）。
- **工具分层（tier 标注）**：`ToolDef` 加 `tier="atomic"`（**原子工具**＝单功能、不可再分，即目录里每个 `ToolDef`）；`Flow` 加 `tier`（**流程工具** `"process"`＝`STANDARD_FLOWS` 中一个独立功能，如 `preprocess`/`translate_page`/`special_page`/`self_check_page`/`export`；**组合工具/流水线** `"composite"`＝多个流程按序串成完整翻译，如 `translate_normal`/`special_pages`/`ai_self_check`/`translate_doc`）。辅助查询：`tool_catalog.atomic_tool_names()`/`tool_tier(name)`、`flow_steps.flow_tier(name)`/`registered_flow_tiers()`（供 Path-B 校验与 UI/提示词使用）。**特殊页默认自动翻译权**：`make_special_pages` 默认逐特殊页走 `AgentStep`（自动翻译，与 `DocumentSession._special_pages` 一致），`negotiate=True` 时逐页 `UserStep` 询问；`translate_doc` 同理。`IfStep.cond` 现可接收 `(rs, params)`（`_cond_accepts_params`）以读取该旋钮——旧的单参 cond 仍兼容。
- **Path B——AI 自由分解（`agent/user_flows.py`）**：`compile_plan(req, llm=None)`→`Plan{tasks:[Task{tier,name,params}], note}`，让模型把一句要求**分解成按顺序执行的若干任务**（可混合原子工具/流程/组合）；`_validate_plan` **以注册表为准**（`tier` 用 `flow_tier`/原子名推断，未知名字丢弃），`run_plan(plan, dispatch=...)` 顺序执行并 fail-closed 在失败步停止并报告；`make_llm_plan_compiler(model, client, log)` 是 AI 分解槽填充——**`llm=None` 时 `compile_plan` 返回空计划、`run_plan` 工具报告“需要模型在线”，绝不降级为确定性单任务**。聊天侧新增 **`run_plan`** 工具（`make_chat_tools` 的 `_dispatch_plan_task` 按 tier 分发：原子→`_ATOMIC`（读/改/核/设置/`re_export`），translate/export 流程→`start_translate`/`re_export`，自检流程→`_audit_scope`；原子 `run_translate` 也路由到 `start_translate`，`run_flow`/`run_plan` 禁止嵌套）；`run_flow` 同样**在 `llm is None`（无模型）时拒绝、不降级为规则解析**，且其自检分支与 `run_plan` 共用 `_audit_scope`（不重复实现）。
- **新增 `render_page`（视觉自检，补上「AI 看不见自己产物」缺口）**：`make_page_executors` 经注入的 `render_handler`（worker 的 `render_page_for_agent`，`what="translation"` 用 `render_translation`，无译文回落源页 `_render_source_page`）把当前处理到该页的译文渲染成 PNG，工具结果带 `image`，`make_llm_decide` 会把任意带 `image` 的结果作为新的视觉观察喂回模型——模型由此能自检溢出/越线/密度，而非靠硬编码质检。
- **`apply_terminology` 真正生效**：`apply_terminology` 写入 `state.user_decisions["terminology"]`，`_translate` 把它作为 `extra_glossary` 传给 `translate_blocks`；引擎把它**合并**到磁盘 `glossary.json` 之上（`extra_glossary` 覆盖同名项），并计入缓存键（术语变化会换缓存）。无 `extra_glossary` 时行为不变。
- **工具的反馈通道（别让模型被"过多反馈"淹没，但错误/图必需）**：每次工具完成后，`make_llm_decide` 给模型的结构化反馈为「**`tool` 消息**」`{"ok", "result", "error"}`（结果只给一次），外加**按需视觉**——`result` 若含 `image` 就以 `image_url` 注入为新的视觉观察（`render_page` 自检、`preview_page` 框选区、开局源页图）。为控上下文（`agent/flow.py`）：①`FlowAgent.observe()` 只报 `last=<tool> ok=<bool>`（`+ error`），**不重放 result**（结果已在 `tool` 消息里）；②`_result_for_message()` 在序列化前**剥离 `image`/字节**（图只走 `image_url`，绝不把 PNG 字节塞进文本）；③`_summarize_result()` 让 `WorkflowState.log` 只存**截断摘要**（默认 120 字符，不再存完整对象/字节）；④`read_page` 支持 `offset/limit` 分页（返回 `total/offset/limit/truncated`），超大页不必一条巨型消息。错误字段始终保留。
- **手写体签字不翻译**（`_drop_signature_items`）：扫描报表底部的签字区（法定代表人：/主管会计工作负责人：/会计机构负责人：行）会被 OCR 成一条「高出一大截、纯字母无数字」的框（手迹墨水范围≈40pt vs 印刷行 ≈9pt，且位于页底约 30% 区域）；若当作文本块，模型会把签字拼音化成 `Xiaobo` 并盖住真实手迹（白矩形遮盖扫描像素——签字是身份不是内容）。检测到即从 `items` 剔除：不翻译、不覆盖、经 `log` 确认；页底高框含数字的（印章外沿/图注）不剔除。**判据必须包含「文本短」（v0.5.29）**：高度 ≥**3×** 行高中位数 + 页底 30% + 含字母 + 无数字 + **去空格后 ≤6 字符**（真机签字实测 2 字符、5.1×/6.4× 行高）；只有前四条时，页脚 `Independent Auditor's Report`（25 字符）与页底大标题（7 字符）会被误剔并谎报为签字（回归 `test_bottom_caption_in_a_tall_box_is_not_a_signature`）；日志为「疑似手写签字 N 处，已跳过（…若非签字请人工确认）」。**在 `_reconstruct_ocr_grid` 之前剔除**——签字不能成为表格单元格；**底部带以未旋转页高为准**（`_ocr_page_blocks` 传 `page.cropbox.height`：OCR 框已由 derotation 映回未旋转 **cropbox** 帧，用 `page.rect.height` 在 90°/270° 页上会拿页宽当页高、把带判到另一侧；`mediabox` 则忽略裁切——回归 `test_ocr_page_blocks_drops_signature_in_a_cropped_page` 锁定 cropbox 语义）；`page_height=None` 时不剔除（无依据宁可保留）。回归 `test_signature_items_are_dropped_from_ocr`、`test_tall_bottom_band_item_with_digits_is_kept`、`test_tall_name_item_outside_the_bottom_band_is_kept`、`test_signature_drop_needs_the_page_height`、`test_dropped_signature_never_reaches_the_grid`、`test_ocr_page_blocks_drops_signature_in_a_rotated_page`。（v0.3.0 曾以「由 AI 运行时决定」为由移除该剔除，但 OCR 块的「保留原文」同样会画白矩形 + 原字形盖住手迹，AI 无从保住扫描墨迹，故恢复。）
**混合页的图内文字（v0.5.44；v0.5.46 起为界面可选项）**：`extract_document_text(..., image_text=None)`（`None`＝**跟随 `ocr`**；界面的「翻译图内文字」勾选框显式传 True/False，`prefs.json` 键 `image_text` 默认 true，`prefs`/`_persist_image_text` 与 `rebuild_table` 同款（`reflow` 自 v0.5.47 起无界面开关、恒为默认开））——原本页 OCR 只在 `not lines or sparse_text` 时跑（`:980`），**有完整文本层的页从不 OCR**，所以烧在位图里的文字（论文柱状图的标题/轴标签、截图、流程图框内文字）既不在文本层也从没被识别，译文里逐像素原样保留（真机：5 页论文样本第 2 页）。现由 `_image_text_blocks` 在图区层面补齐：`_image_regions_for_ocr`（`get_image_info`；单边 ≥24pt、面积 ≥2000pt²、<0.95×页、去重、每页 ≤4）→ `_region_to_array`（**只渲染图区** `get_pixmap(clip=…)`，RGB→BGR 同 `_page_to_array`）→ OCR → `_ocr_items_from_results`（**与扫描页共用的单一校验/数字归一化实现**，自 `_synthesize_ocr_blocks` 抽出）→ `_ocr_plain_blocks`（**不走 `_reconstruct_ocr_grid`**：图里的数字会被当成报表网格重排）→ `replace(in_image=True, image_index=k)` → `_figure_block_bands` 给每块算 `fit_height`（到下一行行顶的真实行距）与 `fit_width`（同行下一块的 x0，否则图片右缘）→ `_merge_ocr_blocks` 并入文本层（互相包含 + 位置去重）。**只对纸面背景的图建块**（整图级 `_region_is_paper`）：照片/彩色底一律不建块、不覆盖、记日志（像素即内容）。导出侧 `_cover_image_text`：覆盖矩形 = **该块自己的 OCR 框**（**不是**扫描页的实测字形带——柱状图的柱子是大块深色游程，按带覆盖会把柱子擦掉；真机柱子像素 100% 保留）∩ 图片 rect，填充色 = `_image_cover_color` 从**图片自身外边距**环采样（图内文字框外围环会被相邻图表元素污染，实测 x 轴刻度标签的环采到了柱子色）；覆盖失败则连译文也不画（不留叠字）。`_photo_ocr_blocks` **排除** `in_image` 块（它们有自己的覆盖路径；块级「是否在照片上」判据对紧贴字形的图内框不成立：字自己就占约一半像素）。图内**纯数字/刻度**不建块；`_fit_block` 对 `in_image` 块用 `fit_height` 作高度预算（换行不越过图里下一行——实测坐标轴标题曾被刻度标签的第二行压住）；**旋转页暂不处理**（`clip` 与 `/Rotate` 先后顺序未实测，跳过并记日志，不冒 v0.5.39 那类采样帧错位的风险）。回归 `FigureTextTest` 4 例。**与语义层的冲突（v0.5.45，必读）**：DocLayout 检出 figure 区域后，`build_structure` 会把**落在该区域框内的所有块**收进 `block_indices`（含图内文字块）→ `ir._role_of` 给它们 `role="figure"` → `ir.translate_ir` 的 `_STRUCTURAL_ROLES=("formula","figure")` 门槛判「保留原文」，于是图内文字虽被识别/覆盖/重画，画的仍是原文（真机 `test_1.pdf`：图区 334 拉丁字符 0 汉字；直连管线无 figure 区域，一直是好的——所以只测直连管线会漏掉）。现 figure/formula 区域**不认领 `in_image` 块**（`build_structure`，图区 bbox 仍覆盖图片本体），并在 `translate_ir` 判定处加同一守卫作 fail-safe；回归 `FigureTextRoleTest` 2 例（含「即使角色被强行设成 figure 也仍进翻译请求」）。**组织架构图 / 架构图有意保留原文（产品决定）**：图表的节点标签**不翻译**。机制是 figure 区域照常认领**扫描页**的 OCR 块（这类块没有 `in_image`）→ `role="figure"` → `translate_ir` 保留原文；真机 `annual report - Mintai Commercial Bank 2025_English.pdf` 第 5 页 58 个节点标签逐字保留（页眉/大标题等图外文本照常翻译，`check_translation --lang English` 会报残留中文——这是**预期**，不是缺陷）。v0.5.45 的例外只放行 **`in_image`**（**有文本层**页面的图内文字 OCR，如论文柱状图），**不**放行普通 `ocr` 块；`translate_ir` 的 fail-safe 同样只管 `in_image`。回归 `ScannedChartRoleTest`（扫描图表保留原文）+ `FigureTextRoleTest`（有文本层页面的图内文字仍要翻译）。**可被 AI 逐块覆盖（第 1 步）**：该保留是**默认策略、不是硬锁**——① `prompts.page_task` 的 `kind == "chart"` 分支明确告诉模型「图内标签保留原文、只译图外文本；本轮用户**明确**要求时才用 `translate_block`/`translate_blocks` 覆盖」；② `DocumentSession._preprocess` 调 `_mark_kept_diagrams()`，把图表页的 **OCR 块**标 `keep_original=True`（导出不覆盖不重画、审计 `_audit_protected` 不当漏译；文本层块如大标题**不标**，仍须翻译）；③ 任何写译文的动作（`set_text`/`translate_block`/`translate_blocks` → `_write`）**清除**该标记 = AI 覆盖默认，`delete_block` / `apply_annotation(action=delete)` **设置**该标记 = 显式保留；④ 导出端的 `chart_page and b.ocr and 译文==原文` 是同一默认在 **IR/直连**路径（无 agent 标记步骤）上的兜底。回归 `test_page_task_keeps_a_chart_diagram_by_default`、`test_preprocess_keeps_a_scanned_diagrams_labels`、`test_write_clears_keep_original_and_delete_sets_it`、`KeptOcrPixelsTest`（默认 + 逐块覆盖）。**导出侧（v0.6.0）**：`classify_page(...) == chart` 的页（≥3 个窄高节点框＝组织结构图/架构图签名）在 `save_translated_pdf` 与 `save_interleaved_pdf` 里对**所有 `ocr` 块既不覆盖也不重画**（`chart_page and b.ocr: continue`），只译文本层块（页眉/大标题）；此前把 OCR 文字白底盖住再重画，在真机年报第 5 页产出 **7 处重叠**（分行名互相压字、最大 73%）并把字号压到 5.0–5.6pt——而原扫描图本身是干净的。现在该页 overlaps / small / rule 全为 **0**，图内 88 个 OCR 块保留原像素（与 `p5_src` 裁剪逐像素一致）。回归 `KeptOcrPixelsTest`。**任何"给区域内的块贴角色/保护"的新逻辑都要先排除 `in_image`。****接线要点（v0.5.46）**：该开关必须在**四条**路径上都传到——`MainWindow` 的两处 `TranslateWorker(...)`（开始翻译、重新导出）、`worker._extract_doc` 的两个提取入口（`extract_document_text` 与 `extract_document_structured`→`extract_structured`，后者**必须**透传，否则 `run.bat` 的 `PDFTRANSLATE_IR_MODE=1` 默认路径下开关失效）、以及 `DocumentText.image_text`（记录提取时用的设置）。**图内块是提取期产物**，所以「重新导出」复用 `last_doc` 前必须比对 `DocumentText.image_text` 与当前开关，不一致就重新提取（`TranslateWorker._run_re_export`）——否则用户刚勾上开关仍会得到未译的图（或取消后又多出图内块）。

- **AI 内容策略：哪些内容保留原文由模型判定（第 2 步，`translate_app/policy.py`）**——「第 1 步」只让 **agent** 路径能逐块覆盖图表默认，**IR 管线与确定性回退路径上没有任何 AI 决策点**（`ir.translate_ir` 的 `figure`/`formula` 门槛、导出端 `chart_page and b.ocr` 兜底全是硬编码）。现在 `TranslateWorker._content_policy` 在**两条路径**上都先问一次模型，候选块只有两类：① `role == "figure"` 且非 `in_image`（默认保留 → 可放行翻译）、② `ocr` 且非 `in_table` 且未被保留（默认翻译 → 可保留，对应扫描件上的印章/公章/签名手迹/二维码）。**红线块从不进入候选**（`_verbatim` 数字/金额、`formula`、表格单元格、`is_chart`），所以模型既看不到也改不动它们。提示词 `prompts.content_policy_task`（每页 ≤ `_MAX_PER_PAGE`(6)、全篇 ≤ `_MAX_CANDIDATES`(120)，未列出的块一律沿用确定性默认）要求回 `{"release": [...], "keep": [...], "reason": "..."}`；`policy.parse_policy_json` 只接受**候选集内**的整数索引（布尔/非整浮点/非数字一律丢弃，防幻觉索引误标块）；`policy.apply_policy` 把 keep 写成 `Block.keep_original`（**导出/审计/prose 分组共用的唯一通道**，一次写入三处生效），release 以 `set[int]` 交给 `translate_ir(release=…)`。**`ir._keeps_source` 里红线优先于 release**：即使决策里写了公式或金额的索引，它们照样保留原文（回归 `test_a_release_never_frees_a_formula_or_an_amount`）。**默认开启**，`PDFTRANSLATE_CONTENT_POLICY=0` 关闭（与其它 IR 旋钮同款，供排查）；**失败方向 fail-open 到确定性默认**（无模型/网络错/坏 JSON/回调抛异常 → 空决策，两条路径与改动前逐字一致）——少保留一个块只是少译一处、用户看得见，误放行一个块会静默毁掉内容，所以模型不可用时绝不动那根指针。`release` 只在用户要求**明确**说要翻译该图时才有意义（提示词写死这一点；无要求时显示「本轮用户要求：（无）」），因为扫描图表被重画会压字、缩字号（v0.6.0 的 7 处重叠实测）。回显日志「内容策略：保留 N 个块原文、放行 M 个图表块翻译（reason）」。回归 `tests/test_policy.py`（21 例：候选筛选/红线排除/上限/JSON 解析/apply/LLM 槽与失败路径）、`test_ir.py`（figure 默认保留、release 后照常翻译、release 放不动公式与金额、`keep_original` 的块既不送模型也不并入段落）、`test_worker.py`（IR 路径接线、release、回调失败 fail-open、环境开关）。

**OCR 数字归一化**（T0）：RapidOCR 会把千分位/小数点看错——`_normalize_number` 只改动**数字形状**的块（`_NUM_SHAPE_RE`），修复逗号小数点互换（`3,702.726,474.45`→`3,702,726,474.45`）、斜杠断裂（`231. / 81`→`231.81`）、逗号写小数（`11,530,351,55`→`11,530,351.55`）；日期（`1960.08`）、纯值（`92.5%`）与无法验证的字符串原样保留。**会计负数也要修（v0.5.30）**：先剥离一层 `(`/`（` 再判定形状、修复后拼回（全角括号归一为 ASCII），否则 `(3,702.726,474.45)` 这类乱码原样输出且**不被识别为数字格**；`_is_numeric_cell` 增加「测试修复后形式」分支（回归 `REAL_CASES` 4 例 + `test_mangled_negative_is_still_a_figure_cell`）。**v0.5.40 两条补充**：① 无千分位的数字也算（`1000`/`1234.56`/`2025`）——此前整列这样的值会让 `_is_numeric_column` 失败、整页被当散文重建；② 附注/序数标记（`(1)`、`（二）`、`八)`）**不算**数字格——否则一列 `(1)(2)…` 就能把双栏扫描散文页判成表格。**旧 OCR 缓存在加载时重归一化**（`extract_document_text` 命中缓存的块经 `replace(b, text=norm)` 修订并记档），因此历史缓存同样修复；未生成读数的逐位校验可交给 `check_translation.py` 的导出后比对。
- 绘制期**数字原子性**（T1）：`_is_amount_atom` / `_is_number_atom` 使 `_break_word` 绝不拆断整数字形（宁可交给单行/溢出处理）——**金额判定必须在拉丁/CJK 断词之前**，否则带币种/单位的金额（`US$1,234,567.89`、`1,234.56万元`、`12,345,678.90元`）会走断词路径被切成 `US$-` / `12,345,678`+`.90元`（读者会以为数字变了；回归 `test_currency_and_unit_amounts_are_never_split`）；**同一判据也适用于「凑行数」的再平衡（v0.5.38）**：`_split_line_half` 只判 `_is_number_atom` 时，两行源格会把 `US$1,234,567.89` 切成 `US$1,23` + `4,567.89`（回归 `test_amounts_are_never_split_by_the_line_rebalancer`）；`_AMOUNT_ATOM_RE` 把前后缀各限 4 字符，含数字的普通词（`iPhone15ProMax`/`PT6A-140`/`COVID-19`）仍照常断词；`_break_latin_word` 每片 ≥2 字符且尾部 ≥2（杜绝 `P- ar- ty` 与 `tiv-`+`e` 碎块）；`_wrap` 合并换行断开的数字（`(?<=\d)([.,])\s+(?=\d)`）。
- **竖排标签**（T1）：`_is_vertical_label`（h ≥ 2w、w ≤ 30、h ≥ 22、单行、非表格）判定中文架构图里的窄高框，`_draw_vertical_label` 经 `TextWriter` 的 `morph` 旋转 90° 绘制（几何与 `insert_text(rotate=90)` 完全相同，见 `_draw_vertical_label` 注释）——**绝不回退到横排逐字碎块**；过长标签按列宽字号居中、越线时**钳制在页内**（页外字形会被提取/渲染裁掉；边界取**未旋转 cropbox 帧**——用 `page.rect` 在 `/Rotate 90` 页上等于拿页宽当页高，会把 y=300..390 的标签推到 y≈160，v0.5.39）。
- `_wrap` 必须按字符断开无空格的 CJK「词」且不丢字——由 `WrapTest` 覆盖。`_break_word` 对剩余字符**单遍累加**字宽、增量跟踪剩余宽度（`text_length` 已验证为纯字宽求和、与逐字求和精确相等、无 kerning，故累加精确），替代了原来「二分查找 + 反复重测整段前缀」的 O(n log n) 做法；长无空格段落提速约 10×（2000 字 `_wrap` ≈ 0.6s → 0.05s）。
- **OCR 表格不再行扩展**（T1）：`save_translated_pdf` 对 `_reconstruct_ocr_tables`（无文本层页重建的网格）**跳过** `_compute_table_layout` 的行高扩展——重建行高与译文测量差太多，曾把 `292,712,933,925.17` 下推约 355pt（回归 `test_ocr_grid_in_place_keeps_rows_below_the_wrapping_cell`）；扫描页保持 OCR 几何，靠单元格行带换行（`fit_height`，见下）+ 缩字兜底——位图表格线与签字墨迹钉死在像素上，无论怎么推挤行都会被察觉。`_reconstruct_ocr_tables` 与 `_reconstruct_ocr_grid` **共用「至少一整列数字」判据**（`_is_numeric_column`：列内过半为数字格，**且至少一个含分隔符/小数点/百分号或 4 位以上**——否则一列 `(1)(2)…` 附注或裸 `1..8` 行次就能把双栏散文页判成表格）——缺了它，双栏扫描**散文页**会被判成表格，`redraw_ocr` 打开时整页被重绘为网格、扫描底图丢失（回归 `test_reconstruct_ocr_tables_rejects_a_prose_page`）。
- **表格格内行数**（T1）：`_fit_block` 的 `in_table` 分支以**源文本**的行数（`block.text.split("\n")`，网格单元格恒为 1 行、文本层表格单元格可为多行）为目标，但**可读性优先于行数**：1 行源 → `_fit_one_line`（缩字不换行，仅当单行 ≥ `_MIN_TABLE_READABLE`）；单行会跌破 6pt 时**改为换行**（旧规则缩到 3pt 保持一行，即扫描报表上 44 格 <5pt 的软肋；本次按需求反转——多行 + 行带限制，见上）；≥2 行源 → `_fit_exact_n`（二分查最大字号使 `_wrap` 行数 ≤ n，再 `_rebalance_to_n` 拆宽行 / `_merge_to_n` 合并短行凑到恰好 n——拆行用 `_split_line_half` 尽量取空格切、决不断整数字形）。曾出现译文 2 行对原文 1 行（行长推挤、数字错位），**改协议前先看 `_fit_exact_n` 相关回归**（`TableCellFitTest.test_fit_block_preserves_two_line_source_count` 等）。
- **OCR 网格的绘制空间（`fit_width`）**：扫描报表的 OCR 框只围住印刷字形——「合并」表头框约 18pt 而真列约 70pt，按框拟合会把 "Consolidated" 压到 3pt（回归 `test_grid_subcolumn_header_gets_row_gap_as_fit_width`）。`_reconstruct_ocr_grid` 由此规则：**bibbox 恒保持自家 OCR 框**（cover/红action 只擦印刷字，绝不擦邻居单元格）；**图格子列里的文本**（合并/母公司表头、签字行标签）`fit_width = 行内下一格 x0 − 2 − x0`（无下一格用页右缘）——译文可用到行内空白；**标签列最左文本格**同理，但以行内下一个单元格（含附注 `(二)` 带、行次数字）为界，长译文在下一格前停下而不被其白色 cover 切成断层（回归 `test_grid_label_fit_width_stops_before_the_note_marker`）；**附注 `(二)`/行次数字**（标签列内嵌套、非最左）保持自家框 + `align="center"`（曾被误扩到整列并压在标签译文上）；**OCR 拆碎的标签碎片**（`营业利润（亏损以` + `号填列）`，间隔 ≤ 20pt、非 `(二)` 式注记）由 `_merge_label_fragments` 合并为一个单元格（回归 `test_grid_merges_split_label_fragments`）。**`fit_height`（行带）**：网格行距不固定——报表各节实测 ≈10pt（利润表「其他综合收益」密集小行）到 ≈23pt（标签行）——行带本身装得下 1–2 行文本，扫描件根本不需要推挤行高（位图表格线、签字墨迹钉死在原位，行扩展必然错位），所以给每个单元格加 `fit_height = 下一行行顶 − 单元格 y0 − 1.5`（**只要存在下一行就设，哪怕行带比字形框还窄**——旧规则 `band > y1-y0` 把密集行静默降级成 `fit_height=0`（=无行带），实测 824 格中 542 格中招、其中 537 格连 2 行 6pt 都装不下；末行 = 0），换行被限制在行带内、停在下一条表格线之前（回归 `test_grid_cells_get_row_pitch_as_fit_height`、`test_tight_rows_keep_a_band_and_stay_inside_it`、`test_fit_block_band_wraps_at_readable_floor_when_band_is_plenty`、`test_fit_block_band_descends_when_the_band_is_tight`；真机实测见下）。**行带连 3pt 单行都装不下时 `_fit_block` 改画单行**（宁可横向溢出到行内空白，也不多行越过表格线）。改动这些语义时递增 `_OCR_CACHE_VERSION`（当前 9：2=加 fit_width、3=标签不再扩 bbox、4=碎片合并、5=加 fit_height——v4 缓存反序列化得 fit_height=0，换行无界、译文会挂进下一行；6=恢复手写体签字剔除，v5 缓存里签字仍是块、会被拼音化并盖住墨迹；7=窄行带不再被丢弃——v6 缓存里密集行 fit_height 恒为 0、等于无行带、多行译文会越过表格线；8=行带下限抬到字形高度；9=行带恢复为真实行距——v8 缓存里的行带被抬高，等于放宽换行预算、第二行会压进下一行，v9 令其重新 OCR）。
**真机实测**（`verify_real_run.py`：配置的 qwen3.8 翻译 p24-27 的 824 表内格，`resume=False` 不写缓存）：42 格多行、0 格触底 3.0（旧规则 5 格）、<5pt 从 44 格降到 29 格。**行带越界统计必须按「推导行带」判定**（`verify_real_run.py`/`check_band_fit.py` 已改为「下一行行顶 − y0 − 1.5」，不再只看 `fit_height > 0`——旧门槛把 542 格被抑制的密集行整批漏掉，使「零越界」成为假绿）：修 v7 后 824 格中 767 格获得行带，网格行**零多行越线**；剩余「超带」全是 3pt 单行落在 <4pt 行带上的 metric 溢出（墨迹仍在行内，脚本单列 `single_over` 统计）。余下 3.1–4.0pt 格全是长英文标签 × 密集行距的**物理极限**（p26 OCI 1–9 子行 ≈9.8pt 行距、p27 现金流量补充行）：行带在 3–4pt 两行与 4pt 单行之间自动选更大字号（`_fit_band` 的二分已涵盖「行内放得下时退回单行」），再大的字号必然越线或丢字——这对扫描件不可回避，而非实现缺陷。附注/行次/数字列逐格核对无误（源扫描本身即有重复 `（四十九）` 三处，OCR 与译文忠实保留）。

### translate_app/main_window.py
- `LANGUAGES` 将界面显示名映射为发送给模型的语言名。语言下拉框**可编辑**，因此必须用 `resolve_language(currentText())` 取值，**不可用 `currentData()`**：输入的文本不匹配任何条目时 Qt 会保留原 `currentIndex`，`currentData()` 仍指向上一次选中项，用户输入会被静默丢弃（请求与输出文件名一起用错语言）。表中没有的语言原样透传给模型。
- 线程生命周期：每次运行创建 `QThread` + worker；`worker.stopped`（在 `run()` 的 `finally` 中发出）连到 `thread.quit`，**同时连到 `worker.deleteLater`**——此刻 worker 线程的事件循环仍在运行，`DeferredDelete` 会被真正处理、C++ 对象被释放；若在 `_cleanup`（线程已结束后）里对 worker 调 `deleteLater`，事件会投给已死的事件循环、永不处理，每次运行泄漏一个 QObject，所以 `_cleanup` 只丢弃 Python 引用（**不要**把 worker 的 `deleteLater` 加回 `_cleanup`）。`thread.finished` 连到 `_cleanup`。**不要改回从 `finished`/`error` 退出线程**——取消时二者都不发，线程事件循环会永远不退出，`_cleanup` 不执行，「开始翻译」按钮永久禁用。关闭窗口即「六亲不认」地强行退出——不弹任何确认框，直接 `thread.terminate()` + `thread.wait()` 强制结束 worker 线程，再用 `os._exit(0)` 硬退整个进程，以绕过 `concurrent.futures` 在解释器退出时对非 daemon HTTP 线程的 join 阻塞（因此绝不等待当前请求完成；缓存的原子写正是为此保底）。
- **worker 生命周期竞态（v0.5.23 修复「异常退出」）**：`stopped -> deleteLater` 在 **worker 线程内**删除 C++ 对象，而 `finished`/`error` 是**排队投给 GUI 线程**的——删除可能先到（实测最小复现 46/60 轮），此时 `self._worker` 仍指向「C++ 对象已删」的包装器。访问**不在实例 `__dict__` 里的** Python 属性会落到 sip 上抛 `RuntimeError: wrapped C/C++ object ... has been deleted`，而 PyQt 对槽内未捕获异常默认 abort **整个进程**（退出码 0xC0000409）——v0.5.21/0.5.22 的 `_on_finished` 读 `_report` 正是如此崩溃，且只在 IR 模式 / 非视觉回退 / 「重新导出」三种路径上出现（它们从不设 `_report`）。两条铁律：①`TranslateWorker.__init__` 必须初始化 `_report = ""`（凡 GUI 会读的字段都要在 `__init__` 里存在，**不要**只在某条运行路径里赋值）；②GUI 侧读字段用 `worker_field(worker, name, default)`（删除后仍能读到包装器 `__dict__` 里的真值），对 worker 做 **Qt 级调用**（`add_user_requirement` 的 `log.emit`、`set_current_page`、`render_translation`、`cancel`）一律先经 `MainWindow._live_worker()`（`sip.isdeleted` 判定）过滤。
- 运行状态闭环：`_start` 重置 `_run_ok` / `_cancelled_by_user` / `_errored` 标志；`_on_finished` 置 `_run_ok=True`；`_on_error` 置 `_errored=True` 并调 `_settle_progress(0, "发生错误")` 把进度条从忙状态（`setRange(0, 0)` 旋转条）落定；`_cleanup` 只在**未成功且未报错**（`not _run_ok and not _errored`）时落定为「已取消」/「就绪」——取消不发 `finished`/`error`，没有这个兜底，进度条会永远旋转、阶段标签停在最后阶段（中途取消的确定性运行也必须落定）。
- **主窗体启动默认位**：`showEvent` 首次进入时对窗口 `move(x+200, y)`——在 Qt/WM 默认摆位（通常居中）基础上**整体右移 200 点**（一次，`_geometry_set` 标志），给预览窗（停靠左、右缘贴主窗体左缘）让出左侧空间。
- OCR 与智能编排已**交由 AI 自动处理，不再提供界面开关**：主窗口不再有「识别扫描页」与「智能编排」两个 `QCheckBox`；`_start` 固定传 `ocr=True`/`agent_mode=True`，扫描页按 `_needs_ocr` 自动触发 OCR（语言自动跟随原文），整篇由 agent 视觉闭环驱动（模型不支持视觉时仍回退确定性流水线）。二者也不写入 `prefs.json`（旧版本残留的 `ocr`/`agent_mode` 键被忽略）。
- **AI 质检已移除，交由 AI 互动处理**：翻译管道中的独立 AI 质检环节——渲染校验/译文质检（`make_rendered_review_fn` + `review_rendered_pages` + `apply_render_adjustments` + `_correct_residual_blocks`）、OCR 整页审查（`make_review_fn`）、OCR 数字核验（`make_verify_number_tool_fn`）——全部删除；这些由 agent 交互层在运行时决定。因此 `TranslateWorker` 不再有 `render_qa` 参数，`extract_document_text` 不再有 `review_fn`/`verify_fn` 参数（`_ocr_cache_path` 也去掉了 review 标记），agent `AGENT_TOOLS` 里也不再暴露 `verify_number`。
- **OCR 表格重建＝0.2.7 版做法（`redraw_ocr`）**：勾选「OCR表格重建为矢量表格」后，扫描（OCR）表格页（≥4 个 OCR 块且 `_reconstruct_ocr_tables` 为真）被**重绘为一张干净空白页**——只画矢量网格线 + 译文，扫描底图/印章/手写签字不再保留；行高按译文扩展（`_draw_ocr_grid_page` 按 OCR 行列簇算边界，用可读下限 6pt 量出每行所需高度并把译文**画进扩展后的行带**——0.2.7 原版把译文画回原 OCR 小框，行高扩展形同虚设，本版按自身注释的意图修正；空白页用**未旋转 mediabox**尺寸并沿用源页 `/Rotate`，否则旋转页的网格线/单元格会落到页外）。模型支持视觉时先由 `make_table_rebuild_fn`（译文 2D 网格）重画为规则 N×M 表（`_draw_ai_table`，可配 `make_merge_tool_fn` 的合并单元格），失败/不可用回退几何重绘。**AI 重建表的单元格按表格拟合（v0.5.28）**：`_draw_ai_table` 的 `cell_block` 用 `in_table=True` + `Block.line_leading=_TABLE_CELL_LEADING`（行距 1.0×、字号下限 6pt、优先单行），行高由 `cell_height` 用**同一套 `_fit_block`** 测量——此前走段落拟合（`in_table=False`）时行距是 1.35×、下限 7pt，两行长标签既压不下也排不开，第二行会越过下面的表格线（用户实测截图）；`Block.line_leading`（0 = 自动）是通用覆盖点，`_line_leading`/`_measure_block_height`/`_draw_translated_block` 均支持，OCR 缓存块缺该字段时取默认值，无需递增 `_OCR_CACHE_VERSION`。v0.4 原先的 `table_vision.py` 矢量重建链（`make_llm_table_structure`/`best_rebuild`/`valid_rebuild`/`score_rebuild`/`_rebuild_ocr_table_blocks`/`_rebalance_rebuild_columns`/`_calibrate_table_grid`/`rebuild_semantics`/`Block.keep_font`）已整体删除，`save_translated_pdf` 的 `rebuild_pages` 参数改为 `redraw_ocr`/`table_rebuild_fn`/`merge_tool_fn`；未勾选时扫描表格仍是原位覆盖 + 行带换行。重译（`retranslate_block`）工具保留；`classify_block` 分类器（连同 `make_agent_tools`）已作为死代码移除。**纯页判定 `_is_pure_ocr_table_page` + 「页面家具」（v0.5.25）**：重绘只保留 OCR 表格单元，所以仅当整页块都是可重绘单元、或**页面家具**时才整页重绘——家具＝非 OCR、非 chart、≤12 字、不含字母/CJK（`str.isalpha()` 对汉字同样为真）、且**位于 OCR 表格 bbox 之外**（表内的无字母短块是文本层碰巧带的表格值，重绘后会落到错误行，仍走原位路径）的块；`_draw_page_furniture` 在重绘页上按原 bbox 补画它们（扫描报表稀疏文本层的页码「22」曾因 P1-6 的「稀疏文本层并入 OCR」使整页判为不纯，导致「OCR表格重建」在这些报表上完全失效——勾了也照样输出扫描表格）。「重新导出」必须与「开始翻译」传同样的旋钮（`rebuild_table`）——旧实现只传 `ocr`/`agent_mode`，勾选后重新导出仍输出扫描表格。`reflow`（表格列宽重排）自 v0.5.47 起**默认生效、无界面开关**（`TranslateWorker` 的 `reflow` 默认 `True`，`PDFTRANSLATE_REFLOW=0` 可关），因此不必再传。
- PDF 可拖放到窗口设置源文件；命令行传入的 PDF 路径效果相同。拖放只接受**本地 PDF 文件**（`dragEnterEvent` 校验 `isLocalFile()` + `.pdf` 后缀；旧实现接受任意 URL，目录/网页链接悬停显示可接受、落下却静默忽略）。
- `chat.py`：**常驻 AI 对话**——`ChatWorker`（`QObject`，moveToThread 到单独的 `QThread`）在后台调用**交互模型**（`interaction_temperature` / `interaction_reasoning_effort`，见 settings）回复侧边栏自由文本；会话由 `ChatSession` 维护多轮 `history`，模型切换时重建。模型在 GUI 线程用 `_selected_model()` 捕获后经 `ask_requested`（queued）传给 worker，worker 不触碰 Qt 控件；`reply_ready`/`error` 回主线程。**对话内容只显示在侧边栏**：主窗口日志只保留流程状态 + AI 工具调用/操作（如 agent 的 `[tool] ok=...`、每页 AI 编排状态、注入要求的操作提示），不显示聊天文本。**对话上下文有界（防小本地模型溢出）**：`ChatSession._window_history()` 每次只给模型尾部 `_CHAT_HISTORY_CAP`(32) 条并裁到**协议安全边界**——`user` 轮或**完整工具组**（`assistant(tool_calls)` + 其全部 `tool` 回复）起，**绝不从 `tool` 消息起**（旧规则「裁到第一个 user，否则回退裸尾部」在单轮工具调用超过 cap 时会交出一个以 `tool` 开头的窗口 → OpenAI 400，整轮工作丢失；窗口内没有边界时退回最后一个 `user` 轮），不再发送累积的全部 `history`；附加/回注的图片经 `_downscale_png` 压到长边 `_CHAT_IMAGE_MAX`(1024) 再发，避免预览整页截图撑爆 `exceed_context_size_error`（**Pillow 在 `requirements.txt` 里但惰性 import**：缺失时经 `log` 告警一次并原样发送，不再静默退化——回归 `test_missing_pillow_is_reported_once`）。**流式输出**：`ChatSession._call(on_text=…)` 在 `on_text` 给出时以 `stream=True` 请求，把每个 `delta.content` 实时回调 `on_chunk`；`ChatWorker` 新增 `reply_chunk` 信号、`MainWindow` 把它接到 `sidebar` 的 `begin_ai_message`/`append_ai_text`/`finish_ai_message`，实现侧栏「打字机」式逐字显示（`_call` 对非可迭代的 mock 响应回退为直接读取，测试不受影响）。
- **交互对话可调用工具（`chat_tools.py` + `doc_context.py`，深化 AI 为中心）**：当加载了 PDF（`doc_ctx.has_source()`）时，`ChatWorker._build_tools` 把**读/导航/改**工具交给交互模型，`ChatSession.reply` 运行有界的 **模型→工具→结果→模型** 循环（上限 `_MAX_TOOL_ROUNDS`=8），直到模型返回纯文本。工具集为 `get_doc_info`/`classify_page`/`read_page`/`goto_page`（读+导航，`goto_page` 经 `preview_bridge.showPreview` 打开预览）与 `set_block_text`/`delete_block_text`/`apply_annotation`（改写/删除受保护译文层；数字格拒绝改写）以及 **`re_export`**（用上次成功译文 + 当前 overlay 编辑重新导出、不重译）。此外还有**只读质检/自定义流程**：`self_check(page, checks)` 对当前译文（`DocContext` 的 last_translated+overlay 合并）跑 `agent.audit_page` 确定性审计、秒级返回 findings（用户说「检查第N页的数字/有没有漏译」时用）；`run_flow(requirement, name)` 把一句中文要求经 `compile_from_user(..., llm=<AI 槽填充>)` 编译成 `FlowSpec` 并对范围内每页跑确定性审计、可传 `name` 以 `save_flow_spec(name, spec)` **登记**为命名流程（磁盘写环境变量门控，见 user_flows；默认只读审计——translate/export 基座重定向到 `run_translate`/`re_export`；**`auto_fix=True` 且重译通道可用时会就地修正**审计发现的问题块并写回受保护覆盖层，返回 `mode/fixed_blocks/failed_blocks/remaining_issue_count`）。槽填充的 `llm` 由 `ChatWorker._build_tools` 经 `make_llm_flow_compiler(session.model, client=session.client)` 注入（复用聊天客户端，不另开连接池）。`re_export` 经 `preview_bridge.reExportRequested` 信号回到 GUI 线程触发 `MainWindow._re_export`（线程安全），返回「已触发」。`chat_tool_hint` 引导模型“确需才调用、不要纯闲聊时调用工具；**改完要最新译文就调 re_export；若不可用就提示用户点主界面「重新导出」按钮，不要说没有导出功能**”。**设置两套参数不变**：对话仍用 `interaction_*`，工具执行不改变请求参数。系统提示词在有工具时追加 `prompts.chat_tool_hint()`（`【可用工具】`），引导模型“确需才调用、不要纯闲聊时调用工具”。
  - **对话侧视觉打通**：聊天工具集中含 **`render_page(page, what)`**——把当前 PDF 某页渲染成 PNG（`what=translation` 盖原文画译文、无译文回落源页；`what=source` 渲染原文），返回 `{image}` 供视觉模型「看」。`ChatSession.reply` 的工具回环会**剥离**工具结果里的 `image`（不塞 base64 进文本），并在下一轮把它以 `image_url` 重新注入为一次**新的视觉观察**（与 agent 侧 `make_llm_decide` 一致）；仅 `model.vision` 为真才注入（非视觉模型仍丢弃图、只发文本）。`run_plan` 的计划原子集含 `render_page`（连同已有的 `apply_annotation`/`retranslate`），`run_plan` 会把任务结果里的 `image` 提升到顶层供回环注入——因此「在计划里先渲染看版面，再改某块」这类多步视觉流程也能走通。
  - **「开始翻译」入口（AI 为中心启动）**：聊天工具新增 `get_settings`（读**线程安全设置快照**——`DocContext.set_settings`/`get_settings`，主窗口 `_refresh_chat_settings` 在源/语言/格式/模型变化时更新，含 source_name/target_language/output_type/output_label/output_path/model/model_id/ocr/agent_mode）、`set_setting(key,value)`（改 target_language/output_type，经 `preview_bridge.setSettingRequested` 在 GUI 线程改下拉+刷新快照）、`run_translate(requirement)`（经 `preview_bridge.translateRequested`→`MainWindow._start(requirement)` 启动，requirement 经 `TranslateWorker(requirements=[...])` **种入** `WorkflowState.requirements`，agent 首决策即可见；**页范围现由模型读取（第 1 步）**——`run_translate` 在 `llm` 可用时调 `agent.ai_scope(requirement, llm)`（复用 `make_llm_flow_compiler` 的槽填充；JSON 里 `scope` + 可选 `reason`），把结果作为 `page_scope` 交给通道，并回显日志「AI 理解为只翻第 N 页（reason）」；**任何解析失败一律 fail-open 到整篇（scope=None）**——静默少翻比多翻更糟。`parse_explicit_scope` 退为**无模型时的离线兜底**（「只/仅/仅限/限定/范围」或显式区间「第2到第5页」；只提到某一页不收窄）。历史：旧实现用 `compile_from_user(...).scope` 把“翻译整篇，第5页图表保留原文”变成只翻第 5 页，其余页原文导出却报成功）。`chat_tool_hint` 引导：用户说“开始翻译/帮我翻译+要求”→ **立刻 `run_translate`**（v0.5.32 起不再先 `get_settings` 汇报设置、不再问“确认开始吗/要重新翻译还是重新导出”、不再复述“即将开始”——**「开始翻译」永远＝全新翻译**；只有用户明确要求改语言/格式时才先 `set_setting`），运行中需要用户决定时 AI 提问。**不自动解析启动**——让 AI 经工具驱动（避免重复启动；`_start` 有 `self._thread` 的互斥兜底）。**「开始翻译」按钮不再直连 `_start`**：`_start_via_chat()` = 置 `_pending_start=True` + `agent_sidebar.send_message("开始翻译")`（`show=True`）→ 显示"我"气泡 + `userMessage` → `_on_user_message` → 交给聊天 AI 走上述入口，因此按钮动作也记录在侧栏对话里；**若这一轮对话结束时 AI 没真的启动**（只反问/只复述），`_on_chat_reply` 末尾的 `_maybe_fallback_start()` 会**直接 `_start()`** 并在日志写明「AI 未启动翻译，已直接开始全新翻译」——用户按了按钮就一定会跑；`run_translate` 已启动时 `self._thread` 非 None（其排队信号先于 `reply_ready` 送达）故不会重复启动；用户随后发别的消息（`_on_user_message`）、对话出错或被取消（`_on_chat_error`/`_on_chat_cancelled`）都会清掉 `_pending_start`，避免无关回复迟点启动翻译。
  - **「局部/定点重译」入口（`retranslate` 工具，新增）**：`make_chat_tools(ctx, translate_texts=…)` 额外注入**翻译侧**批量重译回调（`chat.py` 用 `translator.make_retranslate_batch_fn(session.model, client=session.client, require_vision=False)` 绑定——**复用聊天客户端、不要求视觉**，因它只重译文本块、纯 text→text）。`retranslate(page, indices=None, target_lang=None)` 对指定页（或整页所有可翻译块）批量重译并**写入受保护 overlay**，返回 `{count, indices, translated, failed, note}`——`failed` 是重译失败/返回原文而保留源文的块，**如实列出并提示用 re_export**。数字格/公式块被自动跳过（`_pick_translatable` 只取含字母且非数字格）。这使「把第3页第2块翻成Bank」「第5页整体重译」无需整篇重跑即可就地修正。
  - **`run_flow` 可就地修正 + 失败透明**：`run_flow` 的审计循环按 `spec.auto_fix` 决定只读还是修正——**`auto_fix` 是 opt-in**（v0.5.24 起未指定＝只读，与工具描述/CLAUDE.md 一致；用户须明说“自动改/修一下”，规则解析认 `自动改|自动修正|直接改|帮我改|修一下`，回归 `test_auto_fix_is_opt_in`）——`auto_fix=True` 且注入 `translate_texts` 时，对每页 `audit_page` 报的问题块（`_finding_block_indices` 聚合 residual/missing/numbers/layout 的 `index` 与 table 的 `empty_cells`/`empty_text`）批量重译写入 overlay 并**重审**，返回 `mode=fixed|read_only`、`fixed_blocks`、`failed_blocks`、`remaining_issue_count`；`auto_fix=False`（“只查/不改”）或未接通道时保持只读。既“更广流程”也“失败更透明”——AI 可据此如实告诉用户哪些块没修好。
  - `doc_context.py` 的 `DocContext`：**跨运行存活的文档上下文**——`src_path`、惰性提取的只读 `src_doc`（可 `ensure_doc()`，含 OCR 时较慢，**提取在状态锁之外进行**以免 GUI 的 `set_source` 被阻塞，且仅当源路径未变才回填缓存）、以及**受保护的 `overlay`**（扁平块索引→`{"text":...}` 的译文叠加层，跨运行持久）。`set_source` 变更路径时清空文档+overlay+`_last_translated`，同路径重设保留编辑；`overlay()`/`set_overlay()`/`get_overlay()` 线程安全（`RLock`）。另存 `_last_translated`（上次运行的全量译文）供 `read_page` 展示“真实当前译文”，避免 AI 误改已译好的块。**GUI 线程只准用 `peek_doc()`**（只读已提取结果、绝不触发提取）：预览的「译文」侧曾调用 `ensure_doc()`，markdown/纯文本输出时会在 GUI 线程对整本扫描件 OCR、把窗口冻结数分钟（回归 `test_peek_doc_never_triggers_extraction`）。
  - **叠加层在导出期生效**：`TranslateWorker` 新增 `overlay` 参数；`run()` 在得到 `result` 后调 `_apply_overlay(result)`（内部是 `_apply_overlay_text(translated)`，返回改动数并只记一次告警），把受保护的 `overlay` 文本**覆盖**在本次运行之上（空白条目跳过，绝不把块清空）→ **AI 对话/标注的编辑在导出里一定赢**。随后存 `self._last_translated = list(result.translated)`，`MainWindow._on_finished` 把它喂回 `doc_ctx.set_last_translated`，使聊天能读到真实输出。
  - **「重新导出」（不加重新翻译）**：AI/聊天在运行后改的是 `doc_ctx` 的 overlay，已导出的 PDF 是**当时的**旧版本；重跑「开始翻译」会重新翻译（agent 逐页循环很慢）。主窗口按钮行新增 **「重新导出」**（`_re_export`）：用上一次成功运行的 `_last_translated` + **当前** `doc_ctx.overlay()` 重跑导出。worker 支持 `re_export=True` + `last_translated=…` + **`last_doc=…`（v0.5.41）**，`run()` 走 `_run_re_export()`——跳过翻译流水线：**优先复用上次运行对齐用的 `DocumentText`**。旧实现每次重新 `extract_document_text`（注释写「缓存，便宜」，实则生产无 OCR 缓存：扫描件整篇重 OCR，实测 7–13s/页），且绕过 `structure_mode` 与 `PDFTRANSLATE_OCR_BACKEND`（VLM 后端跑出来的译文会用 RapidOCR 重导），还可能得到不同的块表——随后以 `block_pages … must be parallel lists` 的技术性异常整轮失败。现在无 `last_doc` 时才经 `_extract_doc()` 提取（与首次运行同一路径），配对前校验块数，不一致即提示「上一次的译文不再适用，请重新翻译」；无上次译文则报 error。`_start` 的线程接线抽成了 `_launch_worker(worker)` 供两者复用。仅当源文件未变、且有上次译文时可用（否则提示直接重新翻译）。**「重新翻译」≠「重新导出」（v0.5.31）**：`run_translate`＝重新提取 + 重译 + 导出（**启动时读取当前界面设置**，含「OCR表格重建」），`re_export`＝用**上一次译文**重新生成文件（不重译，导出选项仍读当前勾选）。两者都会在开头打一行 **`导出选项：OCR表格重建=开/关，表格列宽重排=开/关`**（`TranslateWorker._options_line()`，`run()` 与 `_run_re_export()` 各记一次）——给出**生效值**而非勾选状态，供用户/排查确认选项是否真的到达 worker（历史上两次「选项失效」报告都源于「没传到」或「走的不是重译」）。`prompts.chat_tool_hint()` 与 `re_export` 的工具描述都明确要求：「重新翻译/再翻一遍/从头翻译/换设置再翻」必须调 `run_translate`。**生产不落盘缓存**（`PDFTRANSLATE_CACHE_DIR`/`PDFTRANSLATE_OCR_CACHE_DIR` 未设置时不读写磁盘），所以「再次开始翻译」必然从头提取/OCR/翻译，不存在跨运行复用旧结果。**导出直接覆盖目标文件**（v0.5.24 起 `_export` 不再调用 `unique_path` 生成 `name(1).ext`——重复副本让用户分不清哪份最新；唯一例外：输出路径解析后等于源 PDF 时仍另存 `(1)` 并记日志，避免毁掉输入文件）。
  - `MainWindow` 接线：`doc_ctx = DocContext(log=self._chat_log.log.emit)`，`_chat_log` 是 `_LogBridge(QObject)`（信号跨线程排队到 GUI，**绝不在聊天线程直接碰 Qt 控件**）；`set_source_path` / 语言下拉框经 `_refresh_doc_ctx()` 同步上下文（OCR 恒为 True）；`_start` 传 `overlay=self.doc_ctx.overlay()` 给 worker。
- **agent 询问用侧栏（不用模态框，且不用按钮）**：`AnswerBridge.showQuestion` 连到 `sidebar.show_question`——需要用户决定的提问以**自然语言文本**展示，`_AskRow` **不再渲染按钮**，只留一个自然语言输入框（占位“请用自然语言回答…按回车确认”），用户在侧栏**用一句话自由回答**；`answerChosen → answer_bridge.answer` 解锁阻塞在 `AnswerBridge.ask` 的 worker 线程。回答是自由文本，由 AI 解读（特殊页 `make_llm_interpret`；复核模式/导出 `_classify_choice` 关键词；skew 修正先否定后肯定）转成离散决策。**不做模态 `QMessageBox`**：那会盖住用户正在看的预览窗口。
- **提问文本统一为自然语言**：`prompts.special_page_question`/`review_mode_question`/`review_export_question` 都返回开放的、例句内嵌的自然语言问题（`options` 仍作为提示/回归数据返回，但**不作为按钮渲染**）；`agent_interaction_rules` 引导模型“提问要清晰自然、不要依赖按钮、用户会用一句话自由回答”。
- **预览窗口（`preview.py`）**：`SelectionCanvas` 支持**缩放**（滚轮或 ＋/－/适应 按钮；`_zoom` 为适配窗口比例的倍率，`_view_rect` 负责显示矩形），`set_image` 时重置为适配；**左键拖动 = 平移缩放后的页面**（`_panning`/`_pan_start`/`_pan_origin`，图片“跟手”，由纯函数 `clamp_pan` 夹到“图像仍覆盖视口”的范围——轴向上图像未溢出就禁止该轴平移，`fit-to-window` 时左拖是空操作；`resizeEvent` 也按新尺寸重新夹紧），**右键拖动 = 划线标注**（`_active_ink`/`_strokes`：按住右键在图像区拖动就跟鼠标轨迹画自由笔画，存**图像空间**、随缩放贴合页面，半透明红；`clear_ink`/「清除标注」清掉；`set_image` 翻页时清空；**发送时 `PreviewWindow._crop_with_ink` 把这些标记线一并烘进图**——因无框选区，「发送」现在发送**整页图片（含标注）**，`selection` 相关代码已移除）；`PreviewWindow` 增加**上一页 / 下一页 / 跳转**与页码标签（`set_page_info`），导航经 `pageChanged(int)` 交给 `MainWindow._on_preview_page_changed` 更新预览并同步 `worker.set_current_page`。**默认几何**：`_PREVIEW_DEFAULT_W/H=680×900`。**重置只在弹出时**：`show_png(png, reset_geometry=…)` 仅当窗口不可见（新弹出，`MainWindow._show_preview` 以 `not win.isVisible()` 判定）才恢复默认几何/适配视图并重新放置；**窗口内翻页**（上一页/下一页/跳转的本页刷新）保持用户改过的大小与缩放（`set_image(keep_view=…)` 仅当新图像素尺寸相同才保留 zoom/pan，否则重置）——否则翻页一次就把用户拉回默认视图。`place_left_of`：**垂直居中于主窗口**（预览 1/2 高度 = 主窗口 1/2 高度，按 `y = anchor.top() + (anchor.height()-h)//2` 计算，较原「顶部对齐」整体上移/居中）＋**虚拟桌面双向钳制**（原来只钳 x≥0，y 未钳），且主窗口已贴左缘/左侧无空间时改放到**主窗口右侧**（right 缘+1），不再越出屏幕；仅在弹出时调用（原位刷新不动用户的位置）。M5 用户指令：`parse_preview_command`（纯函数）返回 `("goto", 页号0基, what) / ("prev"/"next", None, what)` 或 `None`，识别整条 `上一页/下一页/第 N 页/显示第 N 页/打开[译文|原文]第 N 页/预览[译文|原文]第 N 页`（**支持中文数字**如“第三页”，`_cn_to_int` 转换；`what` 为命令里的 源码/译文 侧），`_on_user_message` 命中时直接驱动预览（并切到对应侧），不再落到“无法打开 PDF”的聊天回复。**预览的「译文」侧**（`_show_preview(what="translation")` → `_render_translation_preview`）：导出后从 worker 记住的**导出 PDF**（`worker._last_pdf`，仅 PDF 输出类型）渲染，源页→输出页按类型映射（`translated_pdf` 同位、`bilingual_pdf` = `2*i+1`，见 `_translation_output_page`）；运行中（agent 未导出）回落 worker 的 `render_translation` 画进行中的译文；两者都没有才回落源页——**修复了“预览只显示原文”**（原逻辑只用 `worker.render_translation`，其 `_agent_state` 在导出后被清空，永远落到源页）。markdown/纯文本无 PDF 输出，译文预览仍回落源页（已知限制）。
- **M6 图片标注回传**：`PreviewWindow._send` 发送 `(cropped_png, pdf_rect)`（`pdf_rect` 把框选从图像像素按 `_dpi=200` 换算成 **PDF 点**，即块坐标系）；`PreviewBridge.get_region` 除 `{png, rect}` 外还带上 `page`；agent 工具 `apply_annotation(page, bbox, text, action)`（`agent/tools.py` 注册、`make_page_executors` 绑定）用 `pdfio.nearest_block(blocks, bbox)` 把标注框映射到**最近块**，`action=set` 写译文 / `delete` 删译文，并记 `Op.user_confirmed`；`make_llm_decide` 现在把**任意返回 `image` 的工具结果**（原仅 `preview_page`）注入为新的视觉观察，使模型看到标注图后按用户指令改写。回归 `NearestBlockTest`/`ApplyAnnotationTest`。
- **预览「发送」也可复制图片给聊天**：`MainWindow._on_preview_send`（由 `_show_preview` 里 `win.sendRequested` 连到）既调用 `preview_bridge.on_region`（保持 M6 agent 标注解锁），又把 `png` 存入 `self._pending_image` 并让侧栏 `add_notice("已复制图片…")`；下一次在侧栏发送文本时 `_on_user_message` 把它连同文本一起经 `ChatWorker.ask_requested(text, model, img)` 交给 `ChatSession.reply(image=…)`——`reply` 对视觉模型把 user content 拼成 `[text, image_url]`，非视觉模型则丢弃图只发文本。`ask_requested`/`ask` 因此升级为 3 参（text, model, image_bytes|None）。
